# STATUS — single source of truth for "where is the project right now"

**Last verified against git:** 2026-09-06 (Chongqing + Guizhou hydro - two loaders stamped modelled hydro `live` through the gap #812's T3 guard left; `stampLive` now refuses any `modelled-fallback` region at any tier. No tier moved. See the entry below. Also 2026-09-06 (Cyprus - a four-month-old decorative TSOC probe replaced with a measured ENTSO-E shape, and PR #280's solar→wind flip disproved; see the Cyprus entry below. Also 2026-09-06 (loader registry - the positional loader wiring that caused the 3-month rotation is gone; both pages now derive their fetch list and payload record from one keyed registry, `src/lib/data-loaders.js`. See the "Loader registry" entry below. Also 2026-09-06 (AEMO per-plant emission gap - 7 of the 10 named plants were being dropped by a noise floor and a 12x energy-unit error; see the 2026-09-06 entry below. Also 2026-09-06 (embed/globe production break - a missing comma killed the paper iframe, and a 3-month-old loader-order rotation was serving six regions the wrong data on the live dashboard too; see the 2026-09-06 entry below. Previously 2026-09-05 (zero-allowlist expiry review - CI had failed every run since 2026-09-01 on an expired review gate, not on breakage; see the 2026-09-05 entry below. Previously 2026-08-20 (honesty / data-label fixes — see the 2026-08-20 entry below: T3-modelled regions no longer stamped `live` [PR #812]; Mexico profile now integrates to its anchor; paper `sourceStatus` description corrected. Earlier 2026-08-19 sweep: the rolling Parquet history was never a time series (**PR #787**), South Africa dead on a stale Eskom URL (**PR #785**), health-alert allowlist incomplete (**PR #784**), `abed` XM capture failing nightly since 2026-08-09 (**PR #786**). Germany creds are **resolved** — they have been in Vercel Production since 2026-08-01. Colombia relay producer and the EIA key rotation still need a human. Previously 2026-07-17: ENTSO-E token 401 fixed, NZ hydro **#470**, Node 20→24 **#487**. Previously 2026-06-25: **#313** Germany measured curtailment; Spain ESIOS parked. Previously: 2026-06-24 data-accuracy sprint #290–#298 + comprehensiveness program #301/#305/#306; #163/#149; #128–#132))))))
**Active branch:** `main` (Vercel production branch; auto-deploys to everylastjoule.com)

## Chongqing + Guizhou hydro stamped `live` - the gap #812's T3 guard left (2026-09-06)

`china-chongqing-hydro` and `china-guizhou-hydro` reached the globe with `sourceStatus: "live"` on data
nothing had fetched. Both loaders run the decorative-probe pattern - fetch `ember-energy.org`, then throw
unconditionally - and return `buildTypicalHydroRegion` output scaled to a hardcoded annual anchor
(0.22 TWh/yr Chongqing, 0.25 TWh/yr Guizhou).

They slipped past the #812 guard on a technicality. `stampLive` downgraded a region only when it carried
`confidenceTier: "T3-modelled"` (or a canonical `estimated` tier with no `confidenceTier` at all), but
`buildTypicalHydroRegion` passes `regionTier: "anchored"`, so its output lands on `T2-annual-calibrated`
and neither test fired. The 2026-08-20 China push hit the same thing and patched `china-hubei-hydro` and
`china-hunan-hydro` by hand (see that entry below); Chongqing and Guizhou were missed.

**The root cause is closed, not just the two files.** `stampLive` now also refuses to stamp any region the
canonical REGIONS table declares `sourceProvenance: "modelled-fallback"`, whatever its confidence tier. It
reads the table rather than the record, because `stampSourceProvenance` runs *after* `stampLive`. The
repo's own provenance gate already classes a live tier paired with `modelled-fallback` as an **impossible**
pairing (`scripts/ci/check-source-provenance-coherence.ts`); this stops `withFallback` minting one.

**Blast radius is exactly the four China hydro regions.** Of the 240 regions declared `modelled-fallback`,
234 are `tier: "estimated"` and were already caught by the existing clauses. The other six are those four
hydro records plus `austria` and `russia-murmansk-wind` - and both of those are built by `statics.json.ts`,
which never calls `withFallback`, so `stampLive` cannot reach them. For hubei and hunan the new clause is a
no-op behind their hand-written overrides.

**Pakistan carried the same latent mislabel.** `pakistan.json.ts` also paired `regionTier: "live"` with a
decorative probe. Its two sub-regions are T3, so the #812 guard already caught them and nothing shipped
wrong - but the option was still a false claim one hydro builder away from the same bug. All three loaders
now pass `regionTier: "estimated"`, matching every other China sibling, and Chongqing and Guizhou also carry
the explicit `sourceStatus: "cached"` / `sourceProvenance: "modelled-fallback"` override that hubei and
hunan use.

**No tier moved.** No `tier:` field in `regions.ts` changed - `china-chongqing-hydro` and
`china-guizhou-hydro` were already `anchored` - and `ci:tally-golden` is untouched (459 regions;
T1a=160 T1b=26 T1c=1 T2=23 T3=249). This is a freshness-label change only: the three regenerated snapshots
differ in `sourceStatus` and `lastSuccessAt` and nothing else, so profiles, `peakGW` and uncertainty bands
are unchanged. As with hubei and hunan, the hydro records' `lastSuccessAt` now reads `2024-01-01` (the
anchor's own date) rather than a build timestamp, because the explicit `cached` override is preserved
ahead of the stamping.

**Guard:** `tests/resilient.test.ts` gains a case that fails on the pre-fix code. Full suite 1213 pass;
`ci:gates` green.

**Follow-up (not in this change):** 26 records across 25 committed snapshots still carry pre-guard stale
`live`. This is the inert caveat logged at #812 - the fallback read path runs `stampCached`, which
overwrites `sourceStatus` unconditionally, so a stale label in the file never reaches the site. Clearing it
wants a full data-refresh build, not a hand edit.
---

## Loader registry — the positional wiring is gone (2026-09-06)

Follow-up to the entry below, which fixed the #203 rotation but left the pattern that allowed it. `src/index.md` and `src/embed/globe.md` each held two parallel structures — an array of ~135 `FileAttachment` promises and an array destructuring that named the results — where **a name was bound to a file by array position alone**. That is what let #203 rotate eight bindings by six slots on both pages and serve nine regions another region's curtailment for three months.

**What changed.** One new module, `src/lib/data-loaders.js`, declares the mapping once: 135 rows of `{ key, file: FileAttachment("../data/<name>.json"), label }`, one row per loader, key and file on the same line. Both pages import it and derive everything from it:

- `src/index.md` — `initLoaderProgress(REGIONS.length, DATA_LOADERS.length)` then `const feeds = await loadDataFiles(DATA_LOADERS, trackFile)`. The 135-entry array and the 30-line destructuring are gone; `regionData` now reads `feeds.<key>`.
- `src/embed/globe.md` — `const feeds = await loadDataFiles(EMBED_DATA_LOADERS)`. `EMBED_DATA_LOADERS` is derived (`DATA_LOADERS.filter((e) => e.embed !== false)`), so the embed's list can no longer fall behind the dashboard's. The one exemption, `zenodo-version`, is now a declared `embed: false` field rather than an omission you have to notice.

`FileAttachment` needs a literal string argument and is resolved at build time, so the paths cannot be computed — but Observable Framework resolves `FileAttachment` inside imported local modules (`observablehq:stdlib`), rewriting each path per importer. That is what makes one shared registry possible. Verified end to end by a full `observable build`.

**Behaviour is unchanged** — this is a refactor. Same 135 files on the dashboard and 134 on the embed, same parallel fetch (every `.json()` is issued before the first `await`), same `regionData` keys and spreads on both pages, same loading-terminal arithmetic (the denominator is now the registry's own length, as `_loaderFiles.length` was).

**Guard.** `tests/globe-drift.test.ts` keeps all four of its jobs; the alignment assertion moved from "name N lines up with file N" to "this row's key kebab-cases to this row's file", which is the same invariant with fewer ways to break it. Re-injecting the #203 rotation into the registry (eight rows, file column rotated six slots) fails it with the file, line and wrong pairing named. Two new guards were added: each page must fetch only through the registry (no page-local `FileAttachment(...).json()` list can be reintroduced), and each page may only read `feeds.<key>` for keys the registry declares.

That test reads the registry as text. `tests/data-loaders.test.ts` covers the runtime side — `vitest.config.ts` now aliases `observablehq:stdlib` to `tests/stubs/observablehq-stdlib.ts`, which keeps each row's literal path on the handle it returns, so the registry is importable outside Observable Framework. It asserts the key → payload mapping, one fetch per registered file, `trackFile` called once per entry (the loading counter only lands on `REGIONS.length` if that holds), all fetches issued before the first `await`, and `EMBED_DATA_LOADERS` excluding exactly the `embed: false` rows. Serialising `loadDataFiles` fails the fan-out assertion, so it is not vacuous.

**Follow-ups (not in this change):**
- The shared `buildRegionData()` extraction. The `regionData` literal is still a hand-maintained copy on each page — the only remaining duplication, and still the reason the parity test exists.
- `anchor` (`data/anchor.json`) is fetched by both pages and read by neither. It has been dead weight since before this change; removing it moves the loader count, so it wants its own PR.
---

## Cyprus — decorative probe replaced with a measured ENTSO-E shape; PR #280 not revived (2026-09-06)

`src/data/cyprus.json.ts` fetched `https://tsoc.org.cy/` and then **threw unconditionally** whether or
not the fetch succeeded, so the "live probe" could never do anything but fall through to
`buildTypicalSolarRegion`. It has been decorative for at least four months — the committed snapshot
records `HTTP 403 for https://tsoc.org.cy/` with `lastSuccessAt 2026-05-04`. This is exactly the pattern
CLAUDE.md rule 3 names ("a live probe that always falls through to fallback is decoration").

**PR #280 (Cyprus TSOC wind relay, Flaresolverr) was reviewed and deliberately not revived.** Three
findings, each verified rather than inferred:

1. **Its fuel change was wrong.** #280 flipped `cyprus` from `kind: "solar"` to `kind: "wind"`. Cyprus is
   solar-dominated by an order of magnitude: over the 30 days to 2026-09-06, ENTSO-E CY delivered
   **0.1512 TWh solar (B16) against 0.0150 TWh wind (B19)**, peak 761 MW against 115 MW; the same ordering
   holds across the 12 months to 2026-09-05. The repo's own anchor is a **PV**-curtailment figure
   (`docs/methodology/anchors.md`; `external-anchors.json` `tsoc-2024-annual`). `kind` stays `solar`.
2. **Its committed CSV could never load.** `data/historical/cyprus-tsoc-wind.csv` held 20 rows that are
   10 unique timestamps duplicated, from a 2-hour window on 2026-06-21 — below the loader's own 24-point
   minimum, so the CSV path never fired. #280's own body admits the loader falls back.
3. **Its `curtailment_mw` column was fabricated.** It is `wind_mw × 0.02`, with the 2% "estimated from the
   gap between installed wind and typical dispatch patterns" — an invented rate shipped as a data column.

**Flaresolverr is available and does work** — a container is running on `abed` (port 8191) and reaches
`tsoc.org.cy` today with HTTP 200 / "Challenge not detected". The dependency was real, not imaginary. It
was rejected anyway because a **non-Flaresolverr route exists and is strictly better**: ENTSO-E publishes
the Cyprus bidding zone (`10YCY-1001A0003J`) A75 at PT30M for both B16 and B19, reachable with the repo's
existing `ENTSOE_API_TOKEN` through the existing `parseEntsoeXml`. Plain `curl` from `abed` also gets 403,
so the TSOC block is browser-fingerprint based, not geographic — and TSOC's page carries **only the
current day** and publishes generation, not curtailment. Choosing it would have meant 30 days of daily
capture through a home-lab browser proxy, for a worse series, in the failure class that cost three weeks
of Colombia capture in 2026-07.

**What shipped:** the loader now derives its diurnal shape from measured ENTSO-E CY solar generation and
scales it to the published annual anchor. It **throws** on an untrustworthy window so `withFallback`
serves last-good.

**MAGNITUDE CALL-OUT (CLAUDE.md rule 3).** `cyprus` moves **0.00822 → 0.01233 TWh/30d** and `peakGW`
0.0432 → 0.0491. The loader's undocumented 0.1 TWh/yr constant is replaced by the **0.15 TWh/yr** figure
the repo already records in `scripts/validation/external-anchors.json` and `docs/validation/cyprus.md` —
the loader and the validation doc had disagreed. Implied curtailment rate against measured PV generation
is 8.2% over the window.

**No tier moved.** `tier: "estimated"` / T3-modelled / `sourceProvenance: "modelled-fallback"` are all
unchanged, `scripts/ci/golden/tier-counts.json` is untouched, and the region is not in the magnitude
baseline. `stampLive` correctly stamps the output `sourceStatus: "cached"`, never `live` — the shape is
measured but the magnitude is an inferred anchor, so this is **not** a live promotion.

**Known caveat, guarded not hidden:** the ENTSO-E CY solar feed is intermittently broken. Peak
hour-of-day means over the last 12 months were 634 MW (2026-08) and 218 MW (2025-12), but collapsed to
20.5 MW (2026-02), 11.3 MW (2026-07) and 8.9 MW (2026-04), with ~95% of intervals near zero. The loader
guards on coverage (≥20 distinct days), magnitude (peak hour-of-day mean ≥50 MW) and peak hour
(05:00–14:00 UTC) and throws otherwise.

Verification: `tsc` clean; 1134 vitest across 200 files (9 in `tests/data/cyprus.test.ts`, all hermetic —
the old test called the network-bound loader); `npm run validate` green; all 9 `ci:gates` green; loader
run live against ENTSO-E, 31 days / 1204 intervals.

---
## Per-region pages + region directory (2026-09-06)

**The dataset now has a route.** `src/index.md` caps its hotspot lists at `HOTSPOT_LIST_LIMIT = 50` per fuel, so at most 150 of the 459 regions were reachable from the site at any hour and the long tail had no path to it at all. Two new surfaces close that:

- **`/regions`** — every region, sorted by country, each row carrying its quality dot, tier, waste modality, and uncertainty band, with client-side search and chip filters (quality / tier / modality / provenance / country). Rows are static HTML from the page loader, so the page is complete with JavaScript off and for a crawler; the filter only shows and hides rows already in the document. The active filter mirrors into the query string, so a filtered view is linkable.
- **`/region/<id>`** — 459 pages from one parameterised page loader, `src/region/[id].md.js`, driven by `dynamicPaths` in `observablehq.config.ts`. Each renders a provenance panel above the fold — tier plus its gloss, the tier's published ± envelope, `sourceProvenance` plus its gloss, the source link — and then that region's `docs/validation/<id>.md` record.

**The uncertainty band was the missing number.** Every `RegionData` record has carried `uncertaintyLowGW`/`uncertaintyHighGW` since the B4 sprint and no surface showed them. The region pages state the tier's envelope — ±15 % T1a, ±50 % T1b, ±35.5 % T1c, ±20 % T2, ±40 % T3 — read from `TIER_DEFAULT_FRACTION` in `src/lib/uncertainty.ts` rather than retyped.

**No live figures on these pages, deliberately.** `RegionData` is composed in the browser from the `src/data/` loaders plus `splitRegion()` arithmetic, and several region ids exist only as the output of that arithmetic. A page rendering a snapshot number beside a live globe is the quiet divergence the CI gates exist to prevent, so each record links to the globe instead. Putting the numbers inline later means first hoisting that composition out of `src/index.md` into a shared `composeRegionData()` — worth doing on its own merits, not as the tail of this.

**The validation docs are read, never regenerated.** `scripts/validation/build_region_docs.py` stamps a fresh `Last updated:` line into all 460 files on every run, so publishing them by regenerating them would have put a 460-file diff in front of every reviewer. The page loader reads `docs/validation/<id>.md` at build time and nothing writes back. The docs' repo-relative links (`../known-limitations.md`, `../../src/data/*.json.ts`) are rewritten to GitHub blob URLs — rendering them unmodified is what produced ~1872 dead links — and `tests/region-pages.test.ts` asserts no `](../` survives into any rendered page.

**`robots.txt` and `sitemap.xml` were never actually deployed.** Framework only emits a non-page file whose path comes out of `config.paths()`, and neither was in `pages` or `dynamicPaths`, so neither has ever appeared in `dist/` — `src/robots.txt` has been advertising a sitemap that 404s. Both are now listed, and the hand-written four-URL `src/sitemap.xml` is replaced by `src/sitemap.xml.js`, which enumerates all 464 URLs and takes each `<lastmod>` from the validation doc's own byline rather than the build clock.

**Build cost, measured on this branch** with network egress blocked so every data loader takes its committed-snapshot fallback and the runs are comparable: **1.7–2.4 s → 82.2 s** with a cold page cache (what Vercel gets, since `prebuild` clears `src/.observablehq/cache`), and **~15 s** on a re-run with the page cache warm. `dist/` grows 6.2 MB → 12 MB, 6 HTML files → 466. Framework validated 1842 links with no warnings, up from 4.

**Not done here:** no link from the dashboard into either surface (`src/index.md` was being rewritten in parallel), no `<link rel="canonical">` or per-page `<meta name="description">`, and no routes for the seven legacy validation-doc ids in `NON_REGION_DOCS` (`iso-ne`, `nyiso`, `india-{north,south,west}`, `italy-south`, `japan`) — they are not canonical regions, so publishing records for them would advertise regions that no longer exist. See the "Region pages" bullet under Known follow-ups.

## AEMO per-plant emission gap + 12x dispatch-interval error (2026-09-06)

`src/data/aemo-per-plant.json.ts` was emitting only **3 of its 10 registry DUIDs** (`aemo-coopgwf1-wind`, `aemo-macarth1-wind`, `aemo-stockyd1-wind`). Both `src/index.md` and `src/embed/globe.md` logged a `[region-data-integrity]` mismatch on every page load naming the other seven, all of which are canonical `tier: "live"` regions in `regions.ts`.

**The console warning was the least of it.** `aemo.json.ts:98` excludes *every* member of `PER_PLANT_DUIDS` from its state aggregates unconditionally — the aggregate-minus-named contract from **#298**. So each per-plant region the loader dropped was curtailment subtracted from the state total and never added back anywhere: **~2,000 MWh of measured NEMWEB curtailment silently deleted** from the dataset per 30-day window.

**Root cause was not "no data for those plants."** Verified against 30 days of NEMWEB `Next_Day_Dispatch` CSVs (window ending 2026-09-05), cross-checked by an independent Python reimplementation that agrees with the loader to floating-point rounding on all 10 plants (max absolute difference 2.3e-17 TWh):

- All 10 DUIDs appear in all 30 daily files — 8,640 dispatch intervals each, every one with `SEMIDISPATCHCAP=1` rows. No missing feed, no unit-map gap, no coordinate gap.
- **Six** of the seven (`wdgph1`, `darlsf1`, `limosf11`, `saphwf1`, `avlsf1`, `bangowf1`) carry **real, positive, measured curtailment** and were dropped by the `MIN_TWH_30D = 0.01` noise floor. These were never zeros — allowlisting them as such would have been a fabricated zero.
- **One** (`ryepark1`) is a genuine measured zero: 176 intervals at `SEMIDISPATCHCAP=1`, but `UIGF − TOTALCLEARED ≤ 0` on every one of them. Capped is not the same as constrained below available.

**Second defect, found in the same pass — a 12x energy overcount.** `totalTWh30d(acc.allPoints)` ran on raw 5-minute dispatch points at the helper's 1-hour default, so every per-plant `totalTWh` read **exactly 12.0x** the curtailed MWh (confirmed on all 10 DUIDs). `aemo.json.ts` avoids this only because it hourly-averages before totalling. The 12x is also what let three plants clear the 0.01 TWh floor while seven did not — the floor and the unit error were entangled.

**MAGNITUDE CALL-OUT (CLAUDE.md rule 3).** The three already-published per-plant regions drop 12x to their true measured values: `aemo-macarth1-wind` 0.1610 → **0.01341**, `aemo-coopgwf1-wind` 0.0845 → **0.00704**, `aemo-stockyd1-wind` 0.0226 → **0.00188** TWh/30d. This is a correction, not a regime change — the prior figures were a unit error on `sourceProvenance: "verified"` measured data. Net effect on dataset totals is a reduction of ~0.24 TWh/30d from those three, partly offset by 0.0020 TWh/30d (2,004 MWh) of newly-emitted curtailment from the six plants that were being dropped - the same energy the aggregate-minus-named gap was deleting.

**Fix (one PR):**
- Points carry `intervalHours = 5/60`, so `totalTWh30d` bills the true dispatch cadence.
- `MIN_TWH_30D` removed. Registry membership is now the sole gate, matching the exclusion set in `aemo.json.ts` exactly. A magnitude filter on that set cannot be correct — it deletes what nothing else counts.
- Region assembly extracted to an exported, pure `buildPerPlantRegions()`; it iterates `PER_PLANT_DUIDS` rather than the accumulator, so a plant with no curtailment emits an all-zero region dated from the feed's latest interval instead of vanishing. A registry DUID with no `AEMO_UNIT_MAP` entry now throws (degrading to last-good) rather than being skipped silently.
- `aemo-ryepark1-wind` added to `ZERO_ALLOWLIST`, `reviewBy: 2027-02-15` — staggered onto an otherwise-empty date, set past the southern-hemisphere spring/summer negative-price season when NSW wind curtailment peaks, with the per-DUID feed-liveness evidence in the note and an explicit instruction to *remove* rather than bump the entry if Rye Park starts carrying signal.
- `data/snapshots/last-good/aemo-per-plant.json` committed for the first time (there was none, so any fresh local build failed `ci:magnitude-golden`), with the 10 matching keys hand-inserted into `scripts/ci/golden/magnitude-baseline.json`.

**No tier moved.** All 10 were already `tier: "live"`; `scripts/ci/golden/tier-counts.json` is untouched and `npm run tally:tiers` is unchanged. This is an emission-set and units fix, not a tier change.

**Gate hazard confirmed (unresolved).** `check-magnitude-golden.ts` tells you on failure to "run `--update` and commit the diff", but the baseline file's own `$comment` warns that `--update` rewrites *every* key from whatever snapshots are on the local machine. Running it here would have silently moved **79 unrelated keys** (halving the Brazil set again — the exact 2026-08-01 incident the warning records — and tripling `germany-tennet-de-wind`) and deleted the warning comment itself. The 10 keys were hand-inserted instead. **The gate's own failure message contradicts the file it writes; worth fixing.**

Verification: `tsc` clean; 1126 vitest across 200 files (9 new in `tests/data/aemo-per-plant-emission.test.ts`); `npm run validate` green; all 9 `ci:gates` green. Rebased onto `origin/main` at 84d752b0, after #921/#922.

---

## embed/globe broken in production - two faults, one of them on the dashboard too (2026-09-06)

`https://everylastjoule.com/embed/globe` - the iframe the da-ri.org paper figure points at - rendered `—%` and a `SyntaxError` instead of the figure. Two independent faults, plus a third found while verifying. **One of them was corrupting the live dashboard as well, not just the embed.**

**Fault 1 - syntax error, embed only (the visible break).** `src/embed/globe.md:313` lost its trailing comma when the 2026-08-21 drift fix (`d7ccab60`) appended a `germany-curtailment` FileAttachment on the line below. The whole `Promise.all` block failed to parse, so the embed rendered the parser error instead of the globe. The reported `Unexpected token (153:2)` is block-relative: block 2 opens at file line 161, so 153 maps to file line 314, the appended line. **Nothing in the toolchain reads these pages as JavaScript** - `tsc` does not see `.md`, vitest does not either, and the `tests/globe-drift.test.ts` guard added by that same PR matched region keys with a regex. So the PR shipped with "tsc clean, 1103 vitest, 5 CI gates green" against a page that could not parse.

**Fault 2 - loader-order rotation, BOTH pages (the silent one).** `#203` (`4905adb0`, 2026-06-17) inserted six India state loaders into the positional `Promise.all` array but appended their names *after* `pakistan, iran` in the array destructuring. Eight bindings were bound to the wrong loader for ~3 months:

- `pakistan` held `india-madhya-pradesh.json`, so `pakistan.wind` / `pakistan.solar` were `undefined` - the two Pakistan regions were reported `malformed` and dropped.
- `indiaOdisha` held `pakistan.json`, a `{wind, solar}` record with no top-level `profile` - reported `malformed` and dropped.
- **Six more regions silently rendered another region's curtailment profile** and no check could see it, because their data was well-formed: `iran` showed Telangana, `india-madhya-pradesh` showed Uttar Pradesh, `india-telangana` showed Punjab, `india-uttar-pradesh` showed Odisha, `india-punjab` showed Chhattisgarh, `india-chhattisgarh` showed Iran.

The three "malformed" regions are **not** a data-shape bug. Both loaders emit correct 24-element profiles; they were being read out of the wrong array slot. Nothing was suppressed to make the check pass.

**Fault 3 - accumulated embed drift.** The embed's `regionData` is a hand-maintained copy of the dashboard's, and had fallen 13 loaders behind: the 8 US utilities (soco, pacw, pace, psco, azps, srp, ipco, tepc), `aemo-per-plant`, `peru-per-plant`, `new-zealand-hydro`, `sichuan`, `guangxi`. Four keys had also gone stale against per-fuel splits upstream (`mexico`, `qinghai`, `yunnan`, `tibet`). The published figure was therefore under-counting even before the syntax error killed it.

**Fixes.** Comma restored; `pakistan, iran` moved to their correct slot in both pages; the embed brought to exact `regionData` parity with `src/index.md` (`zenodo-version` stays out - it is a version badge, not a region, and a live fetch the embed must not block on). `findRegionDataIntegrityIssues` gained a `mismatchedId` category that compares each slot's `regionId` against its key - the detector that was missing for fault 2 - and `tests/globe-drift.test.ts` was rewritten to parse both pages with the TypeScript compiler, assert the destructuring aligns 1:1 with the fetch list, and assert embed/dashboard `regionData` parity. All three faults were re-injected individually and confirmed to fail the new tests.

**Not fixed here:** the shared `buildRegionData()` extraction from `docs/research/2026-06-17-refactor-session-handoff.md` (the parity test makes the duplication safe in the meantime), and the positional-destructuring pattern itself, which is what makes this class of bug possible on both pages. **The positional pattern was removed in the "Loader registry" entry above; the `buildRegionData()` extraction is still open.**

## Zero-allowlist expiry review (2026-09-05)

**CI had failed on every run since 2026-09-01** - the `verify` job on `main` and on both automation branches (`automation/history-snapshot`, `automation/relay-pull`), which is what generated the run of "CI: All jobs have failed" mails. **Not breakage: the review gate fired as designed.** Twenty-two `ZERO_ALLOWLIST` entries were seeded together on 2026-05-12 with a single `reviewBy: 2026-09-01`, so they all expired on the same morning and `npm run validate` exited 1 on the expiry check alone. Every other gate stayed green throughout.

Each of the 22 was reviewed against the committed snapshots rather than date-bumped in bulk. **Seven entries were removed as dead weight, sixteen re-confirmed** (the sixteenth, `japan-okinawa`, was not yet expired but was two days out at `2026-09-07` and would have restarted the mails on the Sunday).

- **Removed - no longer live-tier (2):** `serbia-solar`, `north-macedonia-solar`. Both are `tier: "estimated"` in `regions.ts` since the 2026-06-06 demotion (#119), and the zero check only applies to live tiers, so the entries could never fire. Their own notes predicted this ("likely removable at review").
- **Removed - now carrying signal (5):** `italy-north-zone-wind`, `czech-republic-wind`, `slovenia-wind`, `slovakia-wind`, `moldova-wind`. All five emit positive values on both `main` and the automation branch (1.1e-5 to 8.6e-4 GW). The "below the 1 MW threshold" flooring the notes described no longer exists in `src/lib/entsoe.ts`. **Keeping them would have been the real risk:** an allowlist entry on a region that normally carries signal suppresses exactly the silent-failure alarm the gate exists to raise.
- **Re-confirmed (16), each against evidence of parent-feed liveness,** with the note updated to record it: `aemo-tas-solar` (AEMO alive, 9/10 regions positive); the ten `brazil-*` sub-state allocations (ONS alive, 18/28 positive); `bosnia-and-herzegovina` + `montenegro` (ENTSO-E alive, 52/54 positive); `nyiso-rest-solar` (sibling `nyiso-rest-wind` positive); `japan-okinawa` (eria_jukyu alive, 11/12 areas positive); and `uruguay`, the one region with no sibling to lean on - **verified by a live loader run on 2026-09-05**: adme.com.uy returned 200, the loader parsed the 2026-07 Restricciones Operativas workbook and emitted `sourceStatus: live` with `peakGW 0`. The zero is the true reading, not a dead feed.

**`reviewBy` dates are now staggered** (2027-01-31 through 2027-04-15, alongside the existing 2026-12-25 and 2027-01-15) so the next review arrives as a trickle rather than a 22-entry wall that hard-fails CI. Seasonal zeros are set past their high season: `aemo-tas-solar` to 2027-03-01, past the southern-hemisphere summer solar peak.

**Confirmed still open - `scripts/scorecard.ts` writes tracked files at import time** (the hygiene item in the 2026-08-19 sweep). This session was the fourth to have `data/scorecard.json` and `docs/comprehensiveness-scorecard.md` silently rewritten by a gate run and to revert them by hand. The committed scorecard also claims 461 regions against an actual 459.

---

## Repo-health pass — dead-code cleanup + embed/globe.md drift (2026-08-21)

Two non-data fixes from the rest-of-world coverage research pass:

### 1. Dead-code removal: `CHINA_PROVINCE_CALIBRATIONS`
`src/lib/typical-profiles.ts` held a `CHINA_PROVINCE_CALIBRATIONS` map (inner-mongolia, gansu, qinghai, ningxia, yunnan, tibet) and a `calibrateAnnualTWh()` wrapper that looked it up by **bare province id**. The China loaders were refactored in #819 to use `buildChinaRegionFromAnchor` with **per-fuel ids** (gansu-wind, etc.), so the map keyed on bare ids never matched — `calibrateAnnualTWh` was a pure no-op (verified: no loader passes a bare province id). Removed the map, the function, and its 5 identical call sites. Behaviorally identical — confirmed by 1100 tests + all gates unchanged.

### 2. Embed `globe.md` drift fix
`src/embed/globe.md` (the Observable embed) had drifted from `regions.ts` / `src/index.md` (the live dashboard). The stale audit note (2026-06-17) over-claimed ~44 missing, but most cited regions (Italy zones, Estonia, Mexico, Japan splits, Korea) were already present. The **genuine** remaining drift:
- German TSO zones (`germany-50hertz-*`, `germany-amprion-*`, `germany-tennet-de-*`, `germany-transnetbw-*`, 8 regions) were absent — the loader (`germany-curtailment.json.ts`) was never imported into globe.md, and the two stale `entsoe["germany-wind"/"germany-solar"]` keys pointed at regions the entsoe loader no longer emits.
- Five bare single-region keys (`japan-hokkaido`, `japan-tohoku`, `south-korea`, `inner-mongolia`, `estonia`) had been split into per-fuel ids in `regions.ts` but globe.md still used the old single-region keys.

Fix: imported `germanyCurtailment`, replaced the 2 stale german entries with the 8 TSO zones, and split the 5 bare keys into their per-fuel ids — mirroring `src/index.md`. Added `tests/globe-drift.test.ts` (the audit's recommended "end-to-end integrity" follow-up): parses globe.md's regionData keys, asserts every explicit key is a canonical region id (no stale refs) and that the German TSO zones are present + imported. This guard now catches any reintroduced stale key (verified: re-injecting `germany-wind` fails the test; it parses both quoted and bare keys robustly, including the bare `estonia:` form the first draft missed).

tsc clean; 1103 vitest (3 new) + 5 CI gates + ci:docs-drift + npm run validate green. Independent Claude review pending before merge.

> **For AI sessions:** read this file before drafting plans, brainstorming, or creating worktrees. Plans in `~/.claude/plans/` and `docs/superpowers/plans/` may be SHIPPED — check this file before treating any plan as live work.

---

## Honesty / data-label fixes (2026-08-20)

Two review-driven fixes, both independently code-reviewed (Claude Code review pass). Merged to `main` as `c8ad65d` (PR [#812](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/812), closed).

- **T3-modelled regions were stamped `sourceStatus: "live"`.** `src/lib/resilient.ts` `stampLive` now stamps `T3-modelled` (and any canonical `estimated` tier, even when the loader passed no `confidenceTier`) as `"cached"`, not `"live"`. **Snapshot-lag caveat:** the committed `data/snapshots/last-good/*.json` T3 records (77 T3 records across 63 snapshot files; 60 of them carry the stale `"live"` stamp) keep the old label until the next data-refresh build re-emits them as `cached`. This is a label-semantics change only — `peakGW`/uncertainty bands are unaffected; only the freshness badge/legend and the tooltip `⚠` glyph move (see `region-tooltip.js`). No tier counts moved (golden file untouched).
- **Mexico's profile did not integrate to its anchor.** `src/data/mexico.json.ts` `buildPoints` now normalizes by `shapeSum` (matching `scaleProfileToAnnualTWh`), so the rendered curve integrates to exactly the annual anchor and `totalTWh` (derived from the points, `×30`) agrees with the curve to 1e-9 TWh. Also: `buildPoints` throws on a zero-area shape (was silently emitting a flat-zero profile that dropped the region); the Mexico T3 record is constructed as `cached` at source (previously hardcoded `live` and rescued only by the downstream override; `buildMexicoData()` bypassed that); and the dead `SOLAR_RATE`/`WIND_RATE` constants (never applied — a scalar rate on a shapeSum-normalized profile is a no-op) were removed with a clarifying note. `peakGW` moves to anchor-faithful (solar ~0.09 → ~0.28 GW), which also moves `uncertaintyLow/HighGW`. **Numeric snapshot-lag caveat:** `data/snapshots/last-good/mexico.json` still holds the pre-fix `totalTWh` 0.8/0.4 and `peakGW` 0.0913 — the 12×-inflated and under-scaled figures are what the fallback path and any snapshot-reading consumer serve until the next refresh rebuilds it.
- **Docs corrected:** `docs/paper/02-methods.md` no longer claims T3 `sourceStatus: "live"` means "the probe succeeded" (now: `cached` = modelled proxy / last-good, with the snapshot-lag caveat), and fixes a stale `tier: "static"` reference (it is `tier: "estimated"`).
- Verification: `tsc` clean; `vitest run` 1084 pass; `ci:tier-coherence`, `ci:tally-golden`, `ci:loader-stdout-safety`, `ci:source-provenance-coherence` all pass. (`ci:docs-drift` passes — this PR touches no `docs/validation/*`.)

## China auto-scraper — first promoted region (2026-08-20)

User asked for a system that "automatically scrapes China's publications and updates the database." Built the engine (PR #815, `feat/china-auto-scraper`). **Independent Claude Code review caught four must-fix issues; all fixed before merge.**

- **Source reality (verified):** the repo's China anchors came from a 2024 *annual* NEA press release — not a feed. The authoritative *monthly* source (全国新能源消纳监测预警中心) was unreachable from the dev sandbox (geo/network). The reachable, structured, English backbone is **Ember "China Electricity Data"** (`ember-energy.org/data/china-electricity-data` → GCS `release_chn_subnational_generation_monthly.csv`, real per-province per-fuel monthly generation through 2026-06).
- **What the scraper does:** `scripts/refresh-china.ts` ingests that CSV, annualises the trailing-12-month generation per province+fuel, applies a **published NEA curtailment rate** (弃风率/弃光率), and writes `data/china-anchors.json` (the refreshed data store). `--dry-run` prints the delta; fails non-zero if the fetch fails (no partial writes). It has an `isMain` guard so importing `buildAnchors` in a test does NOT trigger a live fetch + tracked-file write (the scorecard-style hygiene bug).
- **HONESTY — regions stay T3-modelled, NOT promoted to live.** This is the key correction the review forced. Ember's CSV is *generation*, not curtailment, and no live hourly feed exists for China. So `buildChinaRegionFromAnchor()` uses the fresher anchor to rebuild the region but keeps it **T3-modelled** with `sourceProvenance: "modelled-fallback"` (guide §5: a typical-shape profile scaled to a published anchor is T3, never "verified") and `sourceStatus: "cached"` (the anchor is a committed, periodically-refreshed file, not a live fetch this build). `lastSuccessAt` derives from the anchor's `latestMonth`, never `Date.now()`. The uncertainty envelope is recomputed via `applyUncertainty` so `peakGW` stays within `[low, high]`. No tier counts moved (T1b 26, T3 249, total 459).
- **Curtailment-rate table is honest.** Only values **transcribed from a published utilisation figure** in each loader's `source` note are kept (Xinjiang, Qinghai, Yunnan, Tibet, Shandong-solar). Every other province is skipped (`rate===undefined → continue`) — no invented number is attributed to NEA in a committed data file. The store therefore holds **7 anchors** (latest month 2026-06).
- **Value delivered:** *(superseded — see follow-up entry below)* a *fresher, auto-refreshable anchor* for the China regions (2026 trailing-12mo generation instead of the 2024 NEA press release) fed into the existing T3 regions — a genuine data-quality gain, plus the scraper infrastructure. Initially only `xinjiang-wind`/`xinjiang-solar` were wired; the follow-up PR #817 wired the remaining ready anchors (Qinghai, Yunnan, Shandong-solar) through the same helper. It is explicitly NOT a "live" promotion; the user's "upgrade as many as possible" goal for China is bounded by the absence of any public live Chinese curtailment feed. Numerically, Xinjiang's anchors move 5.0→6.341 TWh/yr (wind, +27%) and 3.2→5.94 TWh/yr (solar, +86%) — about +4.1 TWh/yr on dataset totals.
- **Tests (all 1100 pass; 4 CI gates green):** `tests/chinaParse.test.ts`, `tests/refresh-china.test.ts`, `tests/chinaRegion.test.ts` (promote-vs-fallback, envelope in-band, cached/modelled-fallback), `tests/data/xinjiang.test.ts`, `tests/data/qinghai.test.ts`, `tests/data/yunnan.test.ts`, `tests/data/china-shandong.test.ts`, `tests/data/china-followup.test.ts` (all T3-modelled refreshed paths).

## China consistency fixes (2026-08-20)

Two cheap honesty-consistency fixes flagged by the #817 review:
- `src/data/xinjiang.json.ts:35` — its `withFallback` carried a pre-existing `regionTier: "live"` decoration (the loader never fetches live; `run()` throws by design). Corrected to `"estimated"`, matching the sibling loaders. Inert in effect (enrichWithTier skips records that already carry a confidenceTier; stampLive force-demotes T3 to cached) but it was a false intent label.
- `data/snapshots/last-good/china-shandong.json` — regenerated from `buildChinaShandongData()` so it no longer carries the stale `sourceStatus: "live"` on its two T3-modelled records (the exact class of defect #812 banned). `china-shandong-solar` now correctly reads `cached` with the refreshed 4.025 TWh/yr; `china-shandong-wind` carries no sourceStatus (hardcoded 2.5 TWh fallback, no anchor). Scope held to the regions my China PRs touched — the broader pre-existing stale-`live` in other committed snapshots (gansu, ningxia, mexico, pakistan, south-korea, unwired china-*) is inert (overwritten every build) and was logged as a known caveat in #812; a proper fix is a full `observable build` regen, deferred.
- `tsc` clean; 1100 vitest + 5 CI gates (tier-coherence, tally-golden, loader-stdout-safety, source-provenance-coherence, magnitude-golden) green; `npm run validate` (snapshot schema, a separate CI step) also green. NOTE: an initial regen via the loader directly dropped `sourceProvenance` from the wind record and baked a "live probe skipped in tests" note; fixed by regenerating through the real `withFallback` build path (`npx tsx src/data/china-shandong.json.ts`), which stamps both records correctly. Independent review caught this before merge.

## China mixed/solar regions wired (2026-08-20)

Completes the deferred "mixed-kind regions" from the big push. Added a combined-anchor helper `buildChinaMixedRegionFromAnchors` (chinaParse.ts) that consumes the per-fuel wind+solar anchors and combines them into a single `kind: mixed` RegionData, with the same honesty stamping (T3-modelled / cached / modelled-fallback). Wired the 4 remaining China regions that have both a published rate and Ember generation:

- `china-guangdong` (mixed): 0.0076 TWh/30d — was hardcoded 3.2 TWh (≈420× over-estimate; Guangdong barely curtails).
- `china-fujian` (mixed): 0.0020 TWh/30d — was 0.6.
- `china-zhejiang` (mixed): 0.0032 TWh/30d — was 0.8.
- `china-jiangxi` (solar): 0.0262 TWh/30d — was 0.4.

Rates added to `PROVINCE_FUEL_CURTAILMENT_RATE`: Guangdong 0.1%/0.1% (CSG coastal), Fujian/Zhejiang 0.05% floor (bulletin rounds to 100.0% — floor avoids a zero-area profile), Jiangxi 0.5%/1.1% (util 99.5%/98.9%). Store regenerated: **36 → 44 anchors** (8 new: wind+solar for the 4 provinces). `regions.ts` citations updated to the Ember-anchored form with the citable NEA bulletin URL.

**Every province with a published 2024 NEA utilisation rate AND Ember generation is now wired** — the China expansion is at its honest ceiling. Remaining unwired (documented): Beijing/Shanghai/Hainan/Chongqing/Guizhou/Tianjin/Sichuan/Guangxi (no published rate), and Tibet (rate but no Ember generation row).

`tsc` clean; 1100 vitest + 5 CI gates + `npm run validate` green. Independent Claude review pending before merge.

## South Korea Ember-anchored curtailment (2026-08-20)

Replaced the repo's magic-number IEA anchor (0.5 solar / 0.05 wind TWh/yr) for South Korea with a **two-source-cited** estimate, mirroring the China Ember-anchored pattern:

- **Generation**: Ember / Our World in Data, South Korea, 2025 — wind 3.64 TWh, solar 37.80 TWh (`owid-energy.org` "Electricity Data Explorer / Yearly Electricity Data", Ember 2026).
- **Rate**: published 2024 curtailment — 4.1% wind / 3.2% PV in major (mainland) power systems (MDPI 2024, citing IEA; "curtailed wind and PV increased ~55% in 2024"). Jeju excluded, matching the repo's mainland scope.

Resulting curtailed energy: **solar 1.21 TWh/yr** (was 0.5), **wind 0.149 TWh/yr** (was 0.05) — revised upward using an explicit published rate. The repo's prior anchor (0.5/0.05) cited "IEA Korea 2024" without a stated rate; its own rate narrative ("~5% solar / ~2% wind, IEA/KPX") actually implies a few-percent rate, so the new 1.21/0.149 is more consistent with that stated rate than the old magic number was. Mixed years (2025 gen × 2024 rate) follow the same precedent as the China regions.

Changes: `src/data/south-korea.json.ts` (computed anchor from cited constants, blocker note kept — KPX/data.go.kr still needs a Korean-identity serviceKey), `src/lib/regions.ts` (source citations updated to Ember/OWID gen × MDPI/IEA rate, with the KPX key blocker documented), regenerated `data/snapshots/last-good/south-korea.json`. Tier unchanged (`estimated` / T3-modelled / cached / modelled-fallback) — shape is modelled, annual is derived from two published sources. Existing `tests/data/south-korea.test.ts` still passes (structure-based, no hardcoded TWh).

Note: a Korean-government `serviceKey` (본인인증 / Korean identity verification) is the only path to a *live* KPX feed; that was not obtainable, so Korea correctly remains estimated. tsc clean; 1100 vitest + 5 CI gates + npm run validate green.

## China big research push — wired 14 more provinces (2026-08-20)

Following the independent-review discipline, the 2024 NEA monitoring-center (全国新能源消纳监测预警中心) provincial new-energy utilisation bulletin (published 2025-02-06) was transcribed for all 31 provinces. Every province with a per-fuel `kind: wind/solar` loader AND a published utilisation figure now has a refreshed Ember-anchored region:

- Extended `PROVINCE_FUEL_CURTAILMENT_RATE` (refresh-china.ts) with **29 transcribed province+fuel rates** (utilisation → 1−U) from the NEA 2024 monitoring-center bulletin.
- Regenerated `data/china-anchors.json`: **7 → 36 anchors** (the 5 from #815 plus 14 newly-wired provinces, each wind+solar; Tibet carries a rate but no Ember generation row so stays unwired).
- Wired 14 loaders through `buildChinaRegionFromAnchor` (was `buildTypical*`): gansu, inner-mongolia, ningxia, china-hebei, china-shaanxi, china-henan, china-hubei, china-hunan, china-shanxi, china-liaoning, china-jilin, china-heilongjiang, china-jiangsu, china-anhui. Also fixed their inert `regionTier: "live"` decorations → `estimated`.
- Updated `regions.ts` source citations for all 36 wired China regions to the Ember-anchored form (removed stale `NEA 2024 provincial RE monitoring bulletin` text + a dangling `Huaon ...` fragment that #815 had left in xinjiang/qinghai/yunnan/shandong). `sourceUrl` now points at the citable 2024 NEA utilisation bulletin (中国能源新闻网), so the rate half is verifiable.
- Regenerated the **15** corresponding `data/snapshots/last-good/*.json` (14 new + china-shandong, whose wind anchor moved 2.5→1.961) via the real loader path. Fixed two hydro records (china-hubei-hydro, china-hunan-hydro) that `buildTypicalHydroRegion` had stamped `sourceStatus:"live"` — now `cached`/`modelled-fallback`/`T3-modelled`.

**Magnitude call-out (CLAUDE.md rule 3):** the China subtotal moves materially. Annual anchor across the 14 newly-wired provinces ≈36.2 → ≈50.7 TWh/yr (+40%). Notable individual swings: hebei 2.5→8.54 (+242%), ningxia 1.0→3.09 (+209%), henan 1.0→2.88 (+188%), gansu 3.0→7.18 (+139%); conversely jiangsu 2.8→0.265 (−91%) and anhui 2.8→0.079 (−97%) dropped sharply because the old hardcoded figures were over-estimates vs the Ember generation × NEA utilisation product. These are well-cited corrections, not noise. `ci:magnitude-golden` does not cover T3 regions (scoped to LIVE_TIERS), so this call-out is the only signal.

**Honesty contract preserved:** every wired region is `T3-modelled` + `sourceStatus:"cached"` + `sourceProvenance:"modelled-fallback"`. No live promotion. The 2024 utilisation rate is mixed with the 2026M6 Ember generation anchor — the same pattern as #815, consistent with the repo's design and methodology guide §5.

**Deferred (out of scope, documented):**
- Mixed `kind` regions (china-guangdong, china-jiangxi, china-fujian, china-zhejiang) — need a combined wind/solar anchor design; the store only carries per-fuel rows.
- Near-zero placeholder regions (china-beijing, china-hainan, china-shanghai) — curtailment ~0, anchor change immaterial.
- Tibet — has a transcribed rate but no Ember generation row, so no anchor (stays NEA-only, honestly).

- `tsc` clean; 1100 vitest + 5 CI gates + `npm run validate` all green.

## System-performance assessment (2026-08-19 sweep)

An end-to-end health assessment. **Production is fine** - the deployed dashboard is genuinely live (spot-checked `caiso`, `aemo`, `aemo-per-plant`, all carrying `lastSuccessAt` within the hour), CI is green on every run, and ENTSO-E has held since the July token swap. The faults are in the archive and in the alerting around it. Four PRs from this sweep are **merged to `main`**: [#784](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/784) `9449b11d` (health allowlist), [#786](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/786) `f8a90130` (XM chunking + per-metric isolation), [#785](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/785) `42bf2b5e` (Eskom URL resolution), [#787](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/787) `aa8ae79b` (history archive). All five were code-reviewed before merge and the review found real defects in three of them - a duplicate-`regionId` double-write, a stale-record tie-break, a stale-URL preference, and a disproved root cause - all fixed in the branches before landing.

- **The rolling Parquet history was never a time series. PR [#787](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/787).** `data/historical/curtailment_history.parquet` held 854 recorded builds but only **35 distinct global totals**, byte-identical at 21.185715 TWh/30d for the last 139 consecutive builds since 2026-08-03, and flat for 37 days before that. Cause: `scripts/append_history.py` read `data/snapshots/last-good/*.json`, the repo's **committed** corpus, while `data-refresh.yml` only POSTs a Vercel deploy hook and never writes live data back. So each 3-hourly append re-stamped a stale snapshot with a fresh `build_timestamp`. The paper's §3.2 claim that each row "captures the 30-day trailing aggregate at the moment the row was written" was false for every row ever written. #787 repoints the script at the deployed dashboard, adds a `capture_source` column (`committed-snapshot` / `deployed-build`), backfills all 209,400 existing rows without altering any other column, guards against silently falling back to the corpus, and corrects the paper, `03-data-records.md` and `dataset/SCHEMA.md`. **Coverage and totals both move:** 274 region rows per build becomes ~447, and the summed total goes 21.19 to ~32.02 TWh/30d. That rise is real coverage, not double-counting - verified: the largest newly-archived regions are `sichuan` (6.51), `iceland` (0.88), `inner-mongolia-wind` (0.66) and the whole ERCOT and Chinese-province set, none of which the archive had ever recorded. AEMO per-plant regions now appear alongside state aggregates but do not double-count, because #298 made the aggregates aggregate-minus-named; Peru per-plant is absent from the deployed set entirely.
- **South Africa dead on a stale Eskom URL. PR [#785](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/785).** Both ZAF regions had been `degraded` for over two weeks. Eskom's own portal page advertises `.../uploads/2026/07/Total_Hourly_Generation.csv`, which **404s**; the live file had rolled to `2026/08`. The loader's hardcoded `FALLBACK_CSV_URL` pointed at `2026/04`, also dead. #785 resolves the CSV from an ordered candidate list (scraped URL, then the current UTC month and three preceding months), removes the hardcoded dead path, and tolerates the portal page itself being down. Post-fix values are 0.1124 TWh/30d wind and 0.0502 solar, against last known-good 0.1202 and 0.0498 - no magnitude surprise.
- **Health alerting was flapping on an incomplete allowlist. PR [#784](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/784).** Ten health issues opened and auto-closed in 17 days (#624, #635, #674, #684, #695, #723, #731, #764, #771, #781) with an unchanging region list. `KNOWN_ZERO_LIVE_ALLOWLIST` covered 10 of the 12 `japan-*` ids; `japan-chubu` and `japan-tohoku-solar` alone held zero-peak at 6 against a threshold of 5. Both verified live with current timestamps and zero output, identical to their allowlisted siblings. #784 adds them and records the 12-of-12 invariant. This is [#207](https://github.com/honeybeesquad/every-last-joule-dashboard/issues/207)'s failure mode restarting: **treat a repeatedly reopening health issue as a bug in the alert, not as background noise.**
- **Colombia is two separate faults, and worse than the 2026-07-17 entry records.** (1) The relay repo's **daily Colombia and India push stopped on 2026-07-29**, and even the Peru COES push stopped after 2026-08-09. `colombia-vertimientos-daily.csv` still ends 2026-07-28, now 22 days stale against a 4-day threshold, keeping [#620](https://github.com/honeybeesquad/every-last-joule-dashboard/issues/620) open since 2026-08-01. Confirmed on `abed`: there is **no vertimientos or Colombia fetch script at all** in `~/elj-capture` (only `abed-xm-capture.py`, `abed-systematic-probe.py`, `argentina-cammesa-fetch.py`), no clone of `every-last-joule-data-relay`, and no deploy key beyond the personal `id_ed25519`. The producer was never ported off Britta. **Porting it needs a human to register a deploy key on the relay repo.** (2) `elj-capture.service` has failed **every night since 2026-07-27**, always `rc=28` (curl operation timeout) on the `Gene` metric, which is first in `METRICS` - so its exception has aborted the whole run and the other four metrics have not been attempted since. The lake confirms it: `Gene`, `GeneIdea`, `PrecBolsNaci` and `RecoNegEner` all stop at `2026-07.parquet`, `PrecOferDesp` at `2026-05`. **The obvious growing-window explanation is wrong, and was disproved during PR review on 2026-08-19.** The journal shows `Gene` succeeding at a **25-day** window on 2026-07-26 (175,741 rows), first failing at 26 days on 2026-07-27, and then failing at a **one-day** window (`Gene 2026-08-01..2026-08-01`) on 2026-08-02 and at every window since. A single day is a few hundred KB against a 90s `--max-time`, so this is not bandwidth. The failures on 2026-07-27 to 07-30 carry a partial response body while everything from 07-31 returns `out=''` - a transfer that stopped starting, not one that ran out of headroom. **RESOLVED 2026-08-19. Root cause: NordVPN rotated the NordLynx credentials.** `/etc/wireguard/elj-co.conf` is not a config NordVPN issues - it was built by extracting live NordLynx credentials from a connected client, and those rotate. The Bogota server stopped recognising the key, and WireGuard never replies to a handshake it cannot authenticate, so it looked exactly like a dead network. **It was not the subscription** (active until 2027-08-28) and **not the request window**. Fixed by installing the NordVPN CLI on `abed`, connecting to Colombia, re-extracting the key and server endpoint, and rebuilding `elj-co.conf` with the restricted `AllowedIPs` and keepalive. Verified: XM went from `conn=0.000000 http=000` to `conn=0.689 http=405`, and a full five-metric capture landed **252,225 rows for 2026-08**, including `Gene` at 122,420 rows - the metric that had failed nightly since 2026-07-27. July was backfilled, and `PrecOferDesp` advanced from 2026-05 to 2026-07. Recipe and symptoms in `docs/ops/abed-capture-service.md`; it will rotate again. A daily health check (`scripts/relay/elj-capture-healthcheck.sh`, 23:30 NZST via cron on `abed`) now flags a failed unit or a stalled lake, so the next rotation surfaces in a day rather than three weeks. **Original finding, kept for the diagnostic pattern:** Probed directly from `abed` with the tunnel held up. `wg-quick up elj-co` succeeds and installs all three routes (`191.97.0.0/16`, `190.90.0.0/16`, `179.1.0.0/16`), and `ip route get` confirms every one of the four known XM IPs resolves via `dev elj-co`. But **no TCP handshake completes to any of them**: all four return `conn=0.000000 tls=0.000000 http=000` and hang to the full timeout, at both 15s and 90s caps. Packets go into the tunnel and nothing comes back, so the WireGuard peer or its Colombian exit has stopped forwarding. It is not the request window, not XM's API, and not the loader. **This is almost certainly one fault, not two:** the last successful capture was 2026-07-26, the first failure 2026-07-27, the vertimientos CSV's last row is 2026-07-28 and the relay's last daily push 2026-07-29 - every Colombian-egress-dependent job died inside the same three-day window. **Fix the egress before anything else**; porting the vertimientos producer to `abed` will not help while the tunnel carries no traffic. **Next step needs a human:** only `/usr/bin/wg-quick` is NOPASSWD in `abed`'s sudoers, so `sudo wg show elj-co` (last handshake) and `/etc/wireguard/elj-co.conf` (peer endpoint) could not be inspected from an automated session. Run those two, then check whether the Colombian endpoint host or VPN subscription is still alive. PR [#786](https://github.com/honeybeesquad/every-last-joule-dashboard/pull/786) chunks the fetch into 7-day windows, merges partial results instead of overwriting, and fixes the `month_bounds` 1st-of-month case (that part is confirmed by the 2026-08-01 log line `fetch failed Gene 2026-08-01..2026-07-31`). **The chunking will not fix this outage** - a 7-day chunk is larger than the 1-day window that already fails - but wrapping each metric's fetch in try/except is worth having, because it stops `Gene` blocking the other four. **#786 is unverified against the live XM API** - that needs Colombian egress - and deploying it means manually copying the script to `~/elj-capture/` on `abed`. **Do not treat #786 as closing this incident.**
- **WACM decision is cheaper than it looked. Still open.** Both `wacm-wind` and `wacm-solar` contribute **exactly 0.0 TWh/30d**. Removing them therefore costs nothing in magnitude, takes 461 to 459 and T2 24 to 22, and clears two permanently-degraded regions from the alert. Repointing to `SWPW` adds real coverage but changes the footprint and moves totals up. No decision taken in this sweep.
- **SEC-1 still not rotated.** `EIA_API_KEY` in `.env.local` still carries the `DH9J…` prefix, roughly four months after the leak was found. Needs a human.
- **Hygiene: `scripts/scorecard.ts` writes tracked files at import time.** Lines ~456 and ~462 call `fs.writeFileSync` at module top level with no main-module guard, so `npm test` and `ci:bad-conversions-stub` silently rewrite `data/scorecard.json` and `docs/comprehensiveness-scorecard.md`. Three agent sessions each hit this and reverted it manually. Same class as the `build_region_docs` date-stamp churn. Not fixed in this sweep.

**Current health-check state (2026-08-19):** freshness-degraded 4 (`south-africa-wind`, `south-africa-solar`, `wacm-wind`, `wacm-solar`), zero-peak 6. #784 takes zero to 4 and #785 takes freshness to 2, which clears both thresholds.

---

## Relay-stack repair (2026-08-19 evening sweep, continues the morning sweep above)

The NordLynx root-cause fix unblocked a chain of further finds. All verified live; PRs noted where code changed.

- **Colombia egress RESTORED + Britta clobber incident.** After the tunnel fix, Britta's legacy `relay-push.sh` overwrote abed's 21 fresh vertimientos rows on its very next run (it copied its own stale local CSV over the repo file - the exact dual-writer race the migration was meant to end, but only the fetch step had been removed, not the push step). Repaired by revert; Britta's push script no longer touches the Colombia CSV, and abed's fetcher now pulls before reading state and rebases once on a rejected push. Relay repo and dashboard both current through 2026-08-18 (1,749 rows).
- **India gen-re feed was frozen for ~3.5 months by a one-line Python bug.** Britta's `fetchers/india-gen-re.sh` did `with open(path) as f: zf = ZipFile(f)` - the handle closes at the end of the with-block and every later member read raises `ValueError: seek of closed file` (surfaced by a Homebrew Python bump). Fixed on Britta, then **backfilled 103 missing days** (2026-05-08 → 2026-08-17) from CEA's date-addressable report URLs and sorted the CSVs. Dashboard now carries **412 rows per state** (was 309), current through 2026-08-18, incl. new telangana/MP/UP/punjab/odisha/chhattisgarh CSVs in the relay repo.
- **The Colombia failure also blocked India/Vietnam/Taiwan for 3 weeks**: Britta's `cron-wrapper.sh` did `exit 1` when the Colombia fetch failed, so the whole daily push died with it. The Colombia step is now removed (not just disabled) and the wrapper no longer aborts.
- **Vietnam + Taiwan fetchers are scaffolding, not production.** They have NEVER written a CSV to the relay repo: `set -euo pipefail` killed them at an unguarded grep before they logged anything, and even past that Taiwan "completes" without writing output. The pipefail crashes are fixed so they now fail *visibly*; building them out for real (incl. NordVPN egress) is a separate project. Dashboard VN/TW regions are T3 and never depended on them.
- **abed heartbeat → CI chain (PR #793).** abed's nightly healthcheck now pushes `abed-heartbeat.json` to the relay repo; `colombia-relay-pull.yml` copies it in; `relay-freshness.yml` opens the auto-relay-stale issue when the beat is >48h old or `ok:false`. First beat verified live (honestly reporting the pre-fix failed unit; clears after the next successful 21:17 capture). The desktop-notification-only alarm that let 2026-07-27 rot for three weeks is retired as the sole signal.
- **India SLDC recon via NordVPN Indian egress (issue #43 update).** abed's NordVPN unlocks two of the five SLDCs that were dead from NZ: **Tamil Nadu (`www.tantransco.org`) HTTP 200** and **Gujarat (`www.sldcguj.com`) HTTP 200 past the WAF**. AP connects then stalls; Rajasthan/ERLDC still unreachable. The old #43 premise ("needs a genuinely separate egress") is now satisfied by NordVPN on abed - next step is checking whether TN/Gujarat publish machine-readable curtailment, then loaders.
- **Peru COES export FIXED (#800, merged 2026-08-19).** COES redesigned the medidores portal: the old `POST /exportar` -> `"1"` -> `GET /descargar` contract is gone, replaced by a single `GET /Exportar?...` that streams the CSV directly (read out of COES's own `medidores.js`). Two consequences: (1) `scripts/relay/peru-coes-fetch.py` rewritten to the new contract, verified live from abed - 12/12 plants for the 2026-07 window; deployed to Britta's weekly cron the same day. (2) **Publication cadence changed: COES now publishes complete months only** - a window touching the current month returns an empty column-less CSV, so the fetcher's default window now ends on the last day of the previous month. Expect the Peru CSV to advance monthly, not weekly; the relay-freshness thresholds do not watch Peru, so no false alarms.
- **abed's `~/code/every-last-joule-dashboard` is now a real git clone** (was a bare data/scripts skeleton; script deploys were scp'd blind). Deploys are now `git pull`.
- Also opened this sweep: **#795** (WACM removal - T2 25→23, total 461→459, zero totals impact), **#796** (paper stale-figure audit: 9 fixed, 7 flagged for the author - incl. the paper still describing the flare-era 384-region dataset and calling Maharashtra T1a).

---

## Live-data incidents (2026-07-17 sweep)

A build-log audit of production found six feeds silently serving `withFallback` last-good data while the dashboard showed only an amber "degraded" ring. Two are fixed, one is a PR, three are open:

- **ENTSO-E token 401 — FIXED 2026-07-15 (Vercel config, no commit).** ~54 ENTSO-E zones **+ Norway** (which reads the same `ENTSOE_API_TOKEN`) served the **2026-06-17** snapshot for ~4 weeks. Root cause: the token in **Vercel Production** returned **HTTP 401** on every A75 request (57× in one build log); the local `.env.local` token was valid the whole time (200 on the identical request), so the loader code and the API were never at fault — ENTSO-E rotated/revoked the Vercel-side token ~2026-06-17. Fix: synced the working token into Vercel Production + rebuilt. Verified: **0 × 401**, all **54 zones `live`** on everylastjoule.com. The health-check ([#207](https://github.com/honeybeesquad/every-last-joule-dashboard/issues/207)) correctly flagged this daily from 2026-06-16 — alerting worked, the human-response loop was the gap. **Whenever many ENTSO-E regions go degraded at once, grep the Vercel build log for `HTTP 401` first — it's the token, not 60 loaders.**
- **NZ hydro EMI migration — PR #470 fixed the URLs; the ≤$0 signal itself was the real problem (follow-up now shipped, see below).** EMI restructured `DispatchNodalPricesAndVolumes` (~2026-Q2) from flat monthly files to **daily files nested under a year folder** (`…/NodalPricesAndVolumes/YYYY/YYYYMMDD_…csv`); the old monthly URL 404s for every month, so `new-zealand-hydro` fetched nothing and froze at **2026-04-30**. Loader now walks 33 daily files with bounded concurrency (each CSV ~11 MB). **#470 did not restore the feed:** `lastSuccessAt` sat at **2026-05-24** right through to 2026-08-01, because the daily files download and parse fine but contain **no ≤$0 rows at all**. Corrected on 2026-08-01 with evidence: the ≤$0 shortfall is **not** winter seasonality as previously assumed here — nine months of EMI `FinalEnergyPrices` (2025-10 → 2026-06) put hydro-node prices at or below $0 on **one day** (2025-11, 7 rows), flooring at **$0.010** every other month. `FinalEnergyPrices` is also not a substitute source: it has no generation column and no ≤$0 hydro rows either. So the ≤$0 proxy is a **floor** on NZ hydro spill, not a measure of it.
- **NZ hydro honest-zero — FIXED 2026-08-01 (this sweep).** The loader's silent-zero guard now keys on **fetch success, not point count**: ≥25 of 33 daily files parsed ⇒ an empty result is emitted as a **truthful live zero** (`sourceStatus: live`, current timestamp) instead of degrading to a 10-week-stale snapshot still labelled `T1a-live-tso`. Below 25 fetched days it still throws (dataset moved again). Tier deliberately **unchanged** at `live` — the feed is real and current; a T2 `anchored` downgrade would have required inventing an annual anchor, and T3 `estimated` a fabricated profile. `source`/`sourceNote` now carry the floor caveat. **Totals impact: `new-zealand-hydro` goes 0.058 → 0.000 TWh/30d** — the 0.058 was residue of an April snapshot, not a current reading.
- **Germany netztransparenz — RESOLVED 2026-08-01, confirmed 2026-08-19.** `vercel env ls production` now lists both `NETZTRANSPARENZ_CLIENT_ID` and `NETZTRANSPARENZ_CLIENT_SECRET` (created 18 days ago), alongside `ENTSOE_API_TOKEN`, `ERCOT_×3` and `EIA_API_KEY`. Nobody recorded the fix here at the time, so this entry claimed it was open for 18 days. The **secondary** issue below was NOT re-checked in the 2026-08-19 sweep and should be treated as still open. *Historic text follows:* `NETZTRANSPARENZ_CLIENT_ID` / `NETZTRANSPARENZ_CLIENT_SECRET` were **not set in Vercel**, so all 8 DE regions serve the committed *measured* snapshot rather than refreshing live. Re-confirmed **2026-08-01**: `vercel env ls production` lists exactly five vars (`ENTSOE_API_TOKEN`, `ERCOT_×3`, `EIA_API_KEY`) — no Netztransparenz pair; production snapshot frozen at `lastSuccessAt 2026-06-25`. The creds in `.env.local` are **still verified working** (token endpoint 200; loader run 2026-08-01 returned all 8 regions `live`, 564 renewable-curtailment rows for 2026-07, TenneT 552,126 MWh). This is the single remaining action from #313 — fix is `vercel env add` for the two names, then redeploy. **Secondary issue to fix separately:** even with creds the loader logs `zero wind+solar in entsoe snapshot, defaulting to 50/50 split` for all four TSOs, so the wind/solar split is currently synthetic (each pair reports an identical `peakGW`).
- **Colombia — ROOT CAUSE WAS CODE, NOT INFRA. Fixed 2026-08-02.** The earlier "abed unreachable / tunnel down" diagnosis was **wrong**. Verified 2026-08-02: abed is up (5 days), the `elj-capture` timer fires nightly, `wg-quick up elj-co` succeeds and installs the Colombian routes, and the relay CSV is committed and current. The actual bug was in `src/data/colombia.json.ts`: it preferred the live XM API **unconditionally** whenever the call succeeded. XM is documented as geoblocked outside Colombia, but that is not reliable — in the 2026-08-01 production build the Vercel fetch **succeeded** (153 s, no error line in the build log) and returned a series ending **2026-04-03**, four months behind the relay CSV's **2026-07-28**. Reachable-but-stale live data therefore silently overrode fresher relay data and pinned the region to `degraded` with `lastSuccessAt 2026-04-03`. The loader now compares dates and takes whichever source is fresher. **Lesson: "the feed is geoblocked so it will fail" is an assumption, not a guarantee — a partially-working upstream is worse than a dead one, because the failure path never runs.**
- **Colombia — still open, two smaller items.** (1) The vertimientos CSV producer stopped writing after **2026-07-29T18:30Z**: the loader comment attributes it to "Britta cron", and no VertEner/vertimientos job exists on abed, so that cron appears never to have been ported when abed replaced Britta. The CSV is now ~5 days stale against a 4-day threshold, which is why the region still reads `degraded` even after the fix. (2) `abed-xm-capture.py::month_bounds` computes `end = min(last_of_month, today − 1)`, so on the **1st of any month** start > end and the run dies; it self-heals on the 2nd. **Superseded 2026-08-19:** `rc=28` is curl's operation-timeout code and the 1st-of-month case is not its main cause — see the 2026-08-19 sweep for the nightly mid-month failures. This affects the per-plant parquet lake (Power Analyser), not the dashboard region. The lake is otherwise current to 2026-07 for 4 of 5 metrics (`PrecOferDesp` stops at 2026-05).
- **Ireland — NO FIX NEEDED.** The one-off `[ireland] live fetch failed` was a transient network blip; 0 failures across the last 3 builds and all 4 IE/NI regions are `live`. `withFallback` absorbed it correctly.
- **Japan/Turkey/Alberta zero-peak — NOT breakage.** Summer seasonality (JP curtailment ≈ 0 in July). Health-check false positives; do not chase. **Allowlisted 2026-08-01:** 10 JP areas + `new-zealand-hydro`/`new-zealand-solar` added to `KNOWN_ZERO_LIVE_ALLOWLIST` in `health-check.yml`. These held [#207](https://github.com/honeybeesquad/every-last-joule-dashboard/issues/207) open continuously from 2026-06-16 (182 comments), so the two genuine faults in this sweep never re-alerted. Trade-off recorded in the workflow: a real JP loader break during the zero season won't raise the *zero* count, but a failed fetch still degrades to cached and is caught by the *freshness* count.
- **Health-check auto-close is gated on the Germany creds.** Thresholds are `freshness < 10 && zero < 5`. Measured 2026-07-31: freshness 11, zero 15. The allowlist takes zero **15 → 4**, and the NZ hydro fix takes freshness **11 → 10** — still one short. Adding the two Netztransparenz vars drops freshness to **2** (residual: `colombia`, `japan-chubu`) and zero to **2** (`italy-cnord-wind`, `hungary-wind`), at which point #207 closes itself.

- **WACM — dead balancing authority emitting fresh-stamped zeros. Guard added 2026-08-02.** `WACM` and `WAUW` were **retired as EIA-930 balancing-authority codes on 2026-04-01**, absorbed into SPP's Western Interconnection expansion as **SPP West (`SWPW`)**; the last WACM row is 2026-04-02. The loader kept fetching, got an empty window, computed 0 GW, and stamped `sourceStatus: "live"` with a **current timestamp** — indefinitely. Monitoring could not see it: the freshness check trusted the fresh timestamp, and the zero-peak check only inspects live-tier regions, so a T2 region was exempt. `src/lib/eia-iso.ts` now throws when a respondent returns **no rows for either fuel**, so `withFallback` degrades honestly. Verified: WACM → `cached`, SOCO (healthy) still `live`. **Open decision — coverage:** `SWPW` is confirmed live in EIA-930 (2,177 rows, current to 2026-08-01) but its footprint is WACM **+ WAUW + further SPP-west entities**, so it is NOT a drop-in for a region labelled "WAPA Rocky Mountain". Repointing needs either a rename to an SPP-West region (docs + golden churn, and totals will move) or removal of the two WACM regions. Not decided in this sweep.

**Node 24:** `engines` / CI / `.nvmrc` bumped 20→24 (#487) — Vercel deprecated Node 20 and would have **failed all builds from 2026-10-01**.

---

## Tier-promotion survey (2026-08-02) — read before proposing T1 upgrades

Full sweep of all **274 T2/T3 regions** against `docs/methodology/live-data-paths.md`. **Every region at T1 is not achievable**, and the binding constraint is not engineering — it is Test 2: a Path-B rate only counts as T1 if the numerator is **published by the operator itself**. Failure mode 2 disqualifies Ember/IRENA-derived rates outright, because those are back-derived econometrically from generation, making the calculation a double-counted estimate.

Breakdown by blocker: **IRENA-sourced 79** (barred by failure mode 2, no feed exists) · **China NEA provinces 57** (no hourly provincial feed) · **US EIA BAs 19** · **Peru COES 12** (already Path B) · **India 13** · **Korea/Hawaii/misc ~9** · **long-tail single utilities ~90** (probed 12: 5 unreachable, 7 served HTML homepages, none machine-readable).

**US EIA balancing authorities — CLOSED, no promotion possible.** The loaders already fetch live hourly EIA-930 (verified: ~132,000 rows per BA, current to today). The only blocker is the rate. Researched 2026-08-02: **LBNL reports curtailment for the seven ISOs only** — its own figure caption reads `Sources: ERCOT, MISO, CAISO, NYISO, PJM, ISO-NE, SPP`, and the non-ISO West/Southeast buckets that appear elsewhere in the same report are explicitly dropped from the curtailment table. For solar, only CAISO and ERCOT report at all. So there is **no LBNL number for SOCO, PACE, PACW, PSCO, AZPS, SRP, IPCO, TEPC or WACM at any level of aggregation**. Checked each BA's own IRP/FERC/ESG filings too: the only figure found was Xcel/PSCO's Clean Energy Plan **5%–15% curtailment**, which is a forward-looking *modelling assumption* for a hypothetical future portfolio whose value depends purely on which docket you pick — it fails Test 2 (not a measurement) and would fail Test 3 (3× instability) regardless. Idaho Power's cited "PURPA curtailment" anchor appears to conflate curtailment with a *wind QF contract-renewal-rate* assumption and should be re-checked. **All 18 stay T2. Do not re-run this search.**

**India — the geoblock framing is partly wrong; Maharashtra is the real prize.** Probed 2026-08-02. First, a correction that invalidates the [#43](https://github.com/honeybeesquad/every-last-joule-dashboard/issues/43) relay premise: **`abed` shares this workstation's public IP** (Starlink, Christchurch) — it is not an independent vantage point, so it cannot test the Peru/COES "residential IP bypasses the block" hypothesis for India. That needs a genuinely separate egress. Second, **four of eight India `sourceUrl`s were dead domains** (NXDOMAIN at the registry — `msldc.mahavedha.com`, `apsldc.in`, `tnsldc.com`, `sldc.gujarat.gov.in`), not geoblocked; corrected in `regions.ts` this sweep to `mahasldc.in`, `sldc.aptransco.co.in`, `www.tantransco.org`, `www.sldcguj.com`. Third — the find: **`mahasldc.in` is fully reachable with no relay and publishes genuine official curtailment**, under Reports → REMC → Monthly Curtailment Reports: 75 monthly PDFs, each a *daily* wind+solar table with Max MW, Energy MU and free-text cause codes. That supports **`india-maharashtra` T3 → T1b** (daily cadence, not hourly, so T1b not T1a — same reasoning as Colombia). Needs a new loader: CodeIgniter CSRF handshake → `POST /report/fetchReports` → download `mr10_MMYYYY.pdf` → parse the daily table. Still blocked from NZ: Rajasthan, ERLDC, real-APSLDC, real-TNSLDC (TCP timeout), real-GSLDC (WAF 403). Karnataka (`kptclsldc.in`) is reachable but generation-only. The six `gen-re.cea.gov.in` regions are fully reachable and machine-readable but **generation-only**, so T3 is already their honest ceiling — though their committed CSVs haven't refreshed since **2026-05-07** despite the source being trivially reachable (housekeeping gap, not a tier issue).

**Maharashtra — SHIPPED 2026-08-03, T3 → T2 on MEASURED data.** The only promotion the survey found, and it is a magnitude correction as much as a tier move. `mahasldc.in` publishes Monthly Curtailment Reports (`mr10_MMYYYY.pdf`): daily wind+solar curtailed energy with the operator's own cause codes. Parsed **15 published months (2025-01 → 2026-06), 453 daily rows, 33.28 GWh total → ~26.8 GWh/yr**. The superseded loader applied an Ember India rate to CEA generation and emitted **0.101 TWh/30d (~1.23 TWh/yr)** — **~46× the measured figure**, and ~28× the worst single measured month. **Totals impact: Maharashtra effectively stops contributing to the global total.** Landed at **T2, not T1**, because MSLDC publishes monthly, lags 1–2 months, and skips months outright (2025-03, 2025-12, 2026-01, 2026-07 all 404 as at 2026-08-03) — measured data clears T2's bar comfortably, but no live-feed claim survives that cadence. Profile is flat 24/7: the source gives daily energy only, so no intraday shape is claimed. Refresh with `python3 scripts/fetch-msldc-curtailment.py` (needs `pdftotext`; nothing in CI fetches it — run on demand and commit the CSV). **Two bugs fixed alongside:** (1) `readStateSldcCurtailment` summed a trailing-N-day window but divided by the *total* row count, understating by `nRows/window` — a 453-row CSV read at 90 days came out 5× low; fixed at source and at all 12 India call sites. (2) The Maharashtra loader's no-CSV fallbacks emitted a T3-modelled shape, which would have contradicted the new `anchored` tier and tripped `ci:tier-coherence`; they now throw so `withFallback` degrades to the last-good T2 snapshot instead of silently reverting to the 46×-high estimate.

**The IRENA 79 are not a backlog.** They are correctly classified and structurally cannot move: IRENA publishes installed capacity, not measured curtailment, and using an IRENA-derived rate against a generation shape is barred by failure mode 2. Treat them as **done at the right tier**, not as pending work.

**Realistic ceiling: T1 + roughly 0–14 more regions, not 274.** Maharashtra — the one candidate this sweep found — landed at T2, not T1, once its publication cadence was checked. That is the pattern to expect: the constraint is what operators publish, not loader engineering.

---

## What's shipped on `main`

**Coverage — full world:**
- **459 regions across 191 distinct `country` values** — **renewables-only** (curtailed solar/wind/hydro); flare gas purged 2026-06-18. (Counted from `src/lib/regions.ts` on 2026-09-06 and matching `scripts/ci/golden/tier-counts.json` `total: 459`. This line read "461 regions across 195 countries (every UN member + Taiwan + Palestine)" until then; 461 was the total before **PR #795** removed `wacm-wind` + `wacm-solar`, and `country` is a mix of ISO-3 codes and display names, so 191 is a field tally, not a UN-member count.)
- **Tally golden as of 2026-06-24: T1a=160, T1b=26, T1c=1, T2=24, T3=250 (total 461).** (The 2026-06-24 data-accuracy sprint removed 12 broken Ontario per-plant regions: 473→461, T1a 172→160 — see the shipped entry below.) Counted directly from `src/lib/regions.ts`. Locked by `tests/regions.test.ts` + `scripts/ci/golden/tier-counts.json`. **The `T2-flare` bucket is retired** — the 2026-06-18 flare-gas purge removed all 37 flare regions (34 anchored + 3 estimated offshore: trinidad/guyana/suriname) and the bucket itself (total 468→431). Earlier drift (2026-06-10 golden total 385): EIA-930 9 US BAs +18 T2 (#163); China/India/Chile/Colombia/Ukraine expansion (#203/#206); Germany DE-LU → 4 TSO control areas (#214); Mexico per-fuel split (#216); estonia/south-korea per-fuel splits; GGFR flare per-site split (#220 → 468, since purged).
- **Prior golden (2026-06-10): T1a=149, T1b=10, T1c=1, T2=6, T2-flare=8, T3=211 (total 385).**
- Live at **everylastjoule.com** — Vercel auto-deploys from `main`
- **Routes:** `/` (dashboard), `/methodology`, `/paper`, `/about`, `/embed/globe` (the da-ri.org paper iframe), `/regions` (directory), `/region/<id>` (459 per-region provenance + validation records), `/sitemap.xml`, `/robots.txt`. The last four arrived 2026-09-06 — see the entry at the top of this file.
- Dashboard banner: **"Wasted Energy Database · v1.3.2"** (pulled live from Zenodo version metadata; v1.3.2 minted 2026-06-07, version DOI `10.5281/zenodo.20570864`)

**Data-accuracy sprint (shipped 2026-06-24, PRs #290–#293, #297, #298) — six correctness/honesty fixes, all merged + verified green on `main`:**
- **#290** — sub-hourly interval overcounts: Brazil ONS `totalTWh` **2×** (half-hourly summed as hourly) + Malaysia **6×** (10-min); `intervalHours` now set. Brazil snapshot regenerated 5.17→2.50 TWh/30d.
- **#291** — removed 12 Ontario per-plant regions (`ieso-*`): the loader computed `max(0, AvailableCapacity − Output)` where AvailableCapacity is the plant **nameplate**, so the metric was the capacity-factor gap, not curtailment (~50× over; K2 Wind read 473 MW "curtailment" on a 270 MW plant; 12 plants ≈ 29 TWh/yr > Ontario's entire ~14 TWh/yr wind generation). Province aggregate `ontario-wind/solar` retained. **Tally 473→461 (T1a 172→160).**
- **#292** — ENTSO-E placeholder rate corrections: Portugal solar **10%→0.5%** (~25× over; REN reports ~zero), Portugal wind 3%→0.5%, Spain wind **11%→6.7%**, Spain solar **5.5%→3.9%** (OMIE per-tech 2024). Code-only; prod recomputes 3-hourly. `docs/methodology/entsoe-rates.md` updated.
- **#293** — honesty: Mexico self-contradictory `sourceNote` ("1.2%"→"1.2 TWh/yr") + Hokkaido stale "5.8 GWh" string (measured 3.45) corrected.
- **#297** — India: dormant SLDC 90-day-as-annual bug fixed (prevents a future 4× undercount when an SLDC CSV lands) + honest CEA `source:` strings for AP/Gujarat/Maharashtra/Rajasthan (label now matches the live-computed value).
- **#298** — AEMO per-plant double-count: state aggregates now exclude the 10 named DUIDs (`PER_PLANT_DUIDS` in `aemo-unit-map.ts`), per-plant gated to the same set (**aggregate-minus-named**) → each plant counted once. Code-only.
- **Deferred/documented (not bugs to "fix"):** Peru per-plant double-count (~0.1%; aggregate-minus-named infeasible — COES national is by-fuel + cloud-IP-blocked); 9 ENTSO-E placeholder zones (no citable curtailment figure, honest placeholders + a quarantine list); China Gansu/Ningxia "undercount" (the `CHINA_PROVINCE_CALIBRATIONS` map is intentionally disconnected — wiring it overshoots the NEA national reconciliation).

**Comprehensiveness program (2026-06-24, PRs #301, #305, #306) — depth over breadth.** Breadth (461 regions / 191 countries) is already won, so this four-track push targeted measured-not-modelled, two-sided, and credibility-tracked:
- **Track 1 — measured European conversion: Germany SHIPPED; rest no clean path.** Probed the full ENTSO-E Transparency taxonomy — there is **no curtailment-energy product** (A77 = plant outages, A69 = generation forecast, A63 = thermal redispatch). Corrected the long-standing A77 myth in `docs/methodology/entsoe-rates.md` (**#301**). Measured EU curtailment lives only in national TSO feeds. **Germany UNBLOCKED + SHIPPED:** a netztransparenz API key was obtained, and `germany-curtailment.json.ts` now serves MEASURED renewable redispatch curtailment for the 4 TSO zones (`GESAMTE_ARBEIT_MWH` where `RICHTUNG=reduzieren`/`PRIMAERENERGIEART=Erneuerbar`, per `ANWEISENDER_UENB`), replacing the `gen × rate` proxy — 8 regions proxy→measured, wind/solar apportioned by the prior BNetzA rate ratio (feed has no fuel split), `withFallback` to the proxy; recipe in `[[netztransparenz-api-access]]`. Measured comes in below the old proxy (e.g. tennet-de-wind 0.33→0.145 TWh) and is concentrated in the windy north (Amprion/TransnetBW ≈0, allowlisted). **Prod needs the 2 creds added as Vercel env vars** to refresh live (else it serves the committed measured snapshot). Rest of EU stays honest `gen × rate` proxy (documented as state-of-the-art, not a shortcut).
- **Track 2 — economic/self-curtailment: CONCLUDED, already-captured.** A probe + a stopped pilot showed AEMO flags negative-price self-curtailment as `SEMIDISPATCHCAP=1`, which `aemo.json` already counts — so AEMO's economic curtailment is already in our number (more complete than limitation #1 implied); no clean *additional* dataset exists without the heavy, double-count-prone `BIDPEROFFER` bid-stack method (left as "only if wanted"). CAISO already-captured + OASIS endpoint dead; ERCOT 60-day-lag; others none / modelling forbidden.
- **Track 3 — two-sided data: SHIPPED (#305).** Exposed gross renewable generation (`generationProfile` / `generationTotalTWh`) alongside curtailment for ~67 EIA-ISO + ENTSO-E regions; optional schema fields; invariant `generationTotalTWh ≥ totalTWh` tested. Additive — headline unchanged.
- **Track 4 — comprehensiveness scorecard: SHIPPED (#306).** `npm run scorecard` → `docs/comprehensiveness-scorecard.md` + `data/scorecard.json`: measured 40.6% / anchored 5.2% / modelled 54.2% by region; measured 60.6% of the 18.7 TWh/30d total; provenance + freshness (framed as fallback-corpus age, NOT prod staleness — prod rebuilds 3-hourly). The living credibility metric. **Follow-up (4b): auto re-validate T3 anchors vs IRENA/Ember — needs external data sourcing.**
- **Net finding:** both *measurement-acquisition* tracks hit genuine access walls, so the dataset's measured coverage is near its practical ceiling and the remaining gaps are hard-access, not low-effort; the two *depth* tracks shipped. **Parked items needing a human:** (a) **Spain ESIOS token** — free; email `consultasios@ree.es` subject "Personal token request" (≤24h) → unlocks Spain **measured** curtailment (ERNI / *energía renovable no integrada*, MWh, machine-readable via `api.esios.ree.es`; curtailment = the "Bajar"/downward direction; Spain hit record ~10–11% renewable curtailment summer 2025). Confirmed real by probe 2026-06-25 (NOT an A77 myth); public token-free `apidatos.ree.es` lacks it. The next EU measured prize after Germany; build mirrors `germany-curtailment.json.ts`. Recipe in memory `spain-esios-curtailment-access`. (b) AEMO `BIDPEROFFER` bid-stack → positive-price economic curtailment. **Germany is DONE (#313)** — its only remaining human action is adding `NETZTRANSPARENZ_CLIENT_ID` + `NETZTRANSPARENZ_CLIENT_SECRET` as Vercel env vars (Production) so prod refreshes live; until then prod serves the committed measured snapshot.

**Globe data-quality encoding (PR #128, 2026-06-07):**
- Pillar opacity encodes data-quality bucket: measured 1.0 / anchored 0.8 / estimated 0.62 — fuel hue preserved
- Base dot: solid (measured) / ringed (anchored) / hollow (estimated) / amber dashed ring (degraded >24h stale feed)
- Theme-aware HTML legend (bottom-left, collapsible on mobile). New helpers: `src/lib/region-quality.ts`, `--quality-warning` CSS token
- Also ships: `new-zealand-hydro` loader (EMI nodal pricing, T1a-live-tso) — resolves pre-existing `assertCanonicalRegionData` failure

**Per-version dataset history (PR #129, 2026-06-07):**
- `data/historical/version-history.csv` — 1,437 rows across 8 releases (v1.0.0→v1.3.2), one row per region per version
- `scripts/build-version-history.ts` — default mode (working-tree) + `--backfill` (git tag iteration); `npm run version-history`
- Brazil ONS formula drop (v1.3.2) now auditable in the CSV. DuckDB query examples in `dataset/README.md`; R1.2 note in `dataset/FAIR.md`

**Relay resilience (PR #131, 2026-06-07):**
- `relayFreshness()` + `RELAY_STALENESS_THRESHOLD_DAYS=4` in `src/lib/freshness.ts`; relay-CSV-fed regions (Colombia hydro) self-stamp `sourceStatus: "degraded"` when the newest CSV row ages past 4 days → lights the amber ring instead of silently serving stale data.
- New `.github/workflows/relay-freshness.yml` watches the committed relay CSVs and opens an issue when one goes stale at the source.

**Colombia plant-level data-spine (PR #132, 2026-06-08) — the geo-blocked-data moat, operationalised:**
- **Recon complete + validated** (`docs/research/2026-06-07-colombia-xm-plant-level-findings.md`): XM exposes per-`Recurso` offer price (`PrecOferDesp`, COP/kWh), curtailment (`GeneIdea−Gene`, `RecoNegEner`), classification + capacity. **No geolocation in XM** → external geocode join needed. **Honest verdict: the <$15/MWh, ≥2900hr target is NOT in today's data** (spot floor ~$24.5/MWh; max ~103 material curtailment hrs/yr/plant); today's floor is ~$19–22/MWh Caribbean solar — but curtailment is structurally growing. Value = monitoring the build-up via the moat.
- **Egress:** `abed.local` (always-on Ubuntu) carries the Colombian tunnel (cloned from Britta + `PersistentKeepalive=25`). Runbook `docs/ops/abed-egress-setup.md`; access `[[abed-egress-host]]`.
- **Capture service running:** `scripts/relay/abed-xm-capture.py` → daily `elj-capture.timer` (09:17 UTC) → Parquet lake `~/elj-capture/lake/<metric>/<YYYY-MM>.parquet`, DuckDB-queryable. Runbook `docs/ops/abed-capture-service.md`.
- **Next steps + open decisions:** `docs/superpowers/plans/2026-06-08-colombia-data-spine-next-steps.md` (the handoff). Britta's hydro cron still runs (shared tunnel identity — retiring it is a follow-up).

**Tier taxonomy (refactored in PR #88, 2026-05-10):**
- `kind` (content type): wind, solar, hydro, mixed, geo — orthogonal to tier
- `tier` (data quality): live, live-domestic-anchored, live-neighbour-anchored, anchored, estimated
- `sourceProvenance` (source status): verified, official-lead, modelled-fallback
- Legacy static tier labels are retired. `tier` now carries only data quality (`estimated` / `anchored`). The `flare` kind was removed entirely in the 2026-06-18 renewables-only purge.

**Discipline layer (sprint shipped 2026-05-08, PRs #69-#75):**
- **PR #69** — `sourceProvenance` enum field + CI gate scaffolding (`feat/source-status-enum`)
- **PR #70** — bad-conversions methodology checklist + baseline-metric CI stub (`feat/bad-conversions-checklist`)
- **PR #71** — CEA monthly executive summary formally rejected as a curtailment source (`docs/cea-monthly-rejection`)
- **PR #72** — provenance sweep across all 384 regions; CI gate flipped to enforce (`feat/source-provenance-sweep`)
- **PR #73** — Colombia promoted to verified via Britta XM relay (`feat/colombia-relay-refresh`)
- **PR #74** — India SLDC egress audit: 3/6 SLDCs open to any IP, 3/6 geoblocked (`docs/india-sldc-egress-audit`)
- **PR #75** — cleared two pre-existing tier-coherence failures; `ci:gates` fully green (`fix/tier-coherence-colombia-turkey`)

**Nav + pricing-layer cleanup (shipped 2026-05-09, commit e9d86a7):**
- Paper link added to dashboard nav (was missing despite page existing since PR #83)
- Deprecated price-toggle UI hidden from dashboard; pricing data layer (`price.ts`, `fx.ts`) retained briefly for future use → **layer fully deleted in PR #87 (2026-05-10)**

**Audit-fixes sprint (shipped 2026-05-10, PRs #84-#88):**
- **PR #84** — loader-wiring blockers: Belgium/Peru/SA/WA-SWIS spread fixes; runtime `assertCanonicalRegionData` now invoked at page load (catches Belgium-shape bug class loudly); strengthened integrity check verifies 24-elem profile, not just key presence; dead `ercot-native` fetch removed.
- **PR #85** — 9 Japan regional loaders wired into the dashboard (chubu, chugoku, hokkaido, hokuriku, kansai, okinawa, shikoku, tepco, tohoku — previously declared `tier: "live"` but never fetched).
- **PR #86** — pillar-base-inside-country sweep test for all 384 regions (351 pass, 32 skipped for 110m-omitted islands, 2 `it.todo` for known-bug coords).
- **PR #87** — dead code purge: build-time price/fx data layer fully removed; orphan loaders (`japan.json.ts`, `india-{north,south,west}.json.ts`), unit-toggle.js, caiso-oasis fixture, dead unit-toggle CSS rules; cosmetic sweep — region-count drift fixed across README / observablehq.config / dataset/README / about.md / tests/regions.test.ts.
- **PR #88** — tier-taxonomy refactor (described in section above) + India SLDC scaffolding (`readStateSldcCurtailment` helper + CSV ingestion path on 6 India state loaders, opportunistic, no-op until SLDC CSVs land); `build_region_docs.py` regex fixed to tolerate `sourceProvenance` field; 387 validation docs regenerated, 7 cited docs (alberta-wind + 6 India SLDC) hand-preserved.

**Launch-prep sprint (shipped 2026-05-12, three commits on `main`):**
- **`fix(ui)`** — defined missing `--amber-500` CSS token so the active mode toggle button renders with its intended border and foreground colour (committee review UI-1).
- **`fix(loaders)`** — silent-zero guards for ENTSO-E (`fetchEntsoeZone` throws if every technology returns zero points) and AEMO (`parseAemoDispatchCsv` throws on missing `I,DISPATCH,UNIT_SOLUTION,` header; `run()` throws if 30 days of NEMWEB CSVs produce zero curtailment across all states). Snapshot validator gains a non-zero invariant for T1a/T1b/T1c regions with a seeded `KNOWN_ZERO_LIVE_ALLOWLIST` for 16 currently-known-legitimate zeros (committee review DATA-1, DATA-2, DATA-3).
- **`fix(hokkaido)`** — removed the dead `juyo_01` parse path (column[3] is all-renewables MW, not solar 万kW; the previous decode over-counted 10× and mis-attributed mixed fuel to solar). Loader now always returns `buildTypicalSolarRegion` against the OCCTO FY2024 anchor with a sourceNote that names the actual upstream column (committee review DATA-4).

**Audit follow-up sprint (shipped 2026-05-10, PRs #89-#92):**
- **PR #89** — corrected coordinates for `guinea` (was in Atlantic ~50km west of Guinea-Bissau, looked like a copy-paste from the adjacent row) and `guatemala-siepac` (was inside Honduras east of Tegucigalpa). Pillar test grew from 351 active + 2 todo → 353 active passing.
- **PR #90** — Japan upstream investigation: `chubu` (denki-yoho.chuden.jp dead, migrated site has no solar CSV), `tepco` (filename rename `juyo-d-j.csv` → `juyo-d1-j.csv` but new file is demand-only; viable monthly CSV exists at `eria_jukyu_YYYYMM_03.csv` with direct `太陽光出力制御量` column — non-trivial loader rewrite for future), `hokkaido` (loader was parsing all-renewables MW as solar 万kW = 10× overcount + wrong fuel attribution). All three downgraded `tier: "live"` → `"estimated"`. Tally-golden updated. Future work documented in loader JSDocs.
- **PR #91** — pillar-polygon override mechanism. Added `tests/fixtures/region-polygon-overrides.geo.json` mapping region.id → custom GeoJSON polygon. Used for `japan-okinawa`, `jeju`, `vanuatu` whose islands are excluded from countries-110m.json. Test sweep grew from 353 → 356 active passing. No regions remain in the archipelago skip-list.
- **PR #92** — `build_region_docs.py` manual-block markers. `<!-- BEGIN MANUAL --> ... <!-- END MANUAL -->` blocks survive regeneration via section-heading anchoring. 19 new tests. Demonstrated on `india-rajasthan.md` (bad-conversion citation block survives byte-identical across regen). Wraps follow-ups: 6 other cited docs not yet wrapped.

**Visual system + theme system + brand:** as previously shipped (Sunfire + Deepcurrent themes, sun-aligned terminator + pillars, scrubbable timeline, mode toggle, tooltips, mobile perf, self-hosted fonts, theme-tokens runtime reader, no-FOUC boot script, themechange repaints). Defaults to 0.5× playback.

**Paper + DOI:**
- Paper drafts ready at `docs/paper/01-06-*.md`
- v1.3.2 dataset metadata points at version DOI `10.5281/zenodo.20570864` (minted 2026-06-07) and always-latest concept DOI `10.5281/zenodo.19835411`. (v1.3.1 version DOI was `10.5281/zenodo.20136284`.)

**Brazil ONS curtailment fix (shipped 2026-05-17, commit eabf8e5):**
- `val_geracaolimitada` was being summed as the curtailment amount; it is the generation *cap* (what ONS allowed the plant to generate). Correct formula is `max(0, val_geracaoreferencia − val_geracaolimitada)`. Rows with empty `val_geracaolimitada` are unconstrained and now skipped.
- Effect: states with many fully-curtailed events (Maranhão, Ceará) were undercounted; states with large partial caps (Piauí 5×, RN/BA/PB/PE ~2×) were overcounted. Snapshot regenerated from live ONS data.
- Audited all other loaders: no other loader has this class of bug (AEMO uses `unconstrained−cleared`; EirGrid/Chile/Colombia use direct curtailment columns; all others use calibrated `generation × rate`).

**Loader-resilience sprint (shipped 2026-06-06, PR #119):**
Diagnosed from persistent Vercel build-log errors (all builds since ~2026-05-13 silently falling back to stale snapshots).
- **Norway NO5 hydro** — `fetchHydroSeries()` now queries both B11 (Water Reservoir) and B12 (Run-of-river) and merges. ENTSO-E reclassified NO5 Bergen/West reservoir hydro from B12; the B12-only query had returned zero for ~6 weeks. NO5 live again.
- **NYISO solar all-zeros** — EIA's NYIS SUN feed returns rows but all-zero values (upstream data-gap, ~24 days). `buildEiaIsoRegionPerFuel.run()` now detects this and keeps wind fresh while synthesising solar from the wind profile × fallbackSplit (`synthesizeSolarFromWind`), instead of degrading both fuels to a stale snapshot.
- **`sourceStatus` accuracy** — `stampLive()` (resilient.ts) now preserves `cached`/`degraded` status set by per-zone internal fallbacks; ENTSO-E + Norway per-zone catch blocks stamp staleness-aware status. Stale sub-regions no longer masquerade as `live`.
- **serbia-solar + north-macedonia-solar reverted live→estimated** — ENTSO-E A75 B16 feed ceased ~2026-05-13. Root cause is structural, not transient: EMS Serbia and MEPSO North Macedonia are non-EU **Energy Community** TSOs; EU Reg 543/2013 does not bind them (EnC Secretariat IR 2023 flagged NMK transparency "well below required levels", 543/2013 not transposed). Both removed from `entsoe.json.ts` ZONES; re-anchored to IRENA RCS 2025 in `statics.json.ts` (Serbia 0.007 TWh/yr; NMK 0.02 TWh/yr — flagged underestimate given NMK 833 MW→1.2 GW growth). `regions.ts` tier→estimated, provenance→modelled-fallback. Validation docs rewritten. (serbia-wind / north-macedonia-wind stay live — B19 reporting is compliant.)

**Japan area-CSV direct curtailment — Phase 1 (shipped 2026-06-07, PR #126):**
- TEPCO (area 03), Chubu (04), Hokkaido (01) promoted `estimated`→`live` (T1a) via a shared `src/lib/japan-area-csv.ts` parser reading the operators' monthly `eria_jukyu_YYYYMM_NN.csv` direct `太陽光出力制御量+風力出力制御量` columns. Tally golden T1a 147→150, T3 214→211. Okinawa `source` string corrected. TEPCO peak 0.53 GW (vs 0.05 TWh/yr prior modelled anchor).

**Japan area-CSV direct curtailment — Phase 2 (in review, feat/japan-area-csv-phase2):**
- Kyushu (09), Kansai (06), Chugoku (07), Shikoku (08), Hokuriku (05) promoted from rate-proxy (×N% calibration) to direct measured curtailment via `eria_jukyu` area CSVs. Okinawa (10) folded onto shared module for consistency. All 10 Japanese areas now read direct `太陽光出力制御量+風力出力制御量` columns. No tier count change (all already live/verified). Kyushu peak 2.14 GW (was ×10% proxy). 6 dead `STATIC_PROFILE_KIND` entries removed.

**Paper v1.3.2 numbers refresh (shipped 2026-06-06, PR #109):**
- Every numeric claim in `src/paper.md` + `docs/dari/paper.html` re-derived against current snapshots after the Brazil ONS formula fix (eabf8e5). Six claims drifted >5%: total verified waste 338.8→**293.7 TWh**, T1 curtailed renewables 184.5→**138.9 TWh** (−25%, Brazil-driven), wasted/Bitcoin 171%→**149%**, curtailed-alone/Bitcoin 93.4%→**70%**, foregone revenue $16.2B→**$14.3B**, priced regions 186→**118 verified**.
- §3 reframed (editorial Option B): leads with flare-dominant verified total (293.7 TWh = 149% of Bitcoin); 53% flare / 47% curtailed renewables. Bitcoin denominator kept at WooCharts 197.6 per paper's stated anchor.
- **Version skew resolved (2026-06-07):** v1.3.2 minted to Zenodo (version DOI `10.5281/zenodo.20570864`) via PR #121 + GitHub release `v1.3.2`. All metadata (`package.json`, `CITATION.cff`, `.zenodo.json`, `README`s, `FAIR.md`, CHANGELOG) now consistent at v1.3.2 / 385 regions. Paper cites the always-latest concept DOI, which now resolves to v1.3.2.

## What's NOT shipped / open PRs

**BUILT + VERIFIED 2026-06-20 (PR pending) — Loader stdout-safety guardrail (follows PR #259).** Two complementary gates so a `console.log` in a `src/data/*.json.ts` loader can't freeze the site again (#259: a loader's stdout IS its emitted JSON, so a stray stdout write corrupted `data/ontario-per-plant.json` → site-wide `SyntaxError` on the dashboard's `Promise.all(FileAttachment().json())`). (1) **Lint gate** `scripts/ci/check-loader-stdout-safety.ts` (pure AST scanner `scripts/lib/loader-stdout-scan.ts`) — AST, **not grep**, because the *fixed* Ontario loader documents the gotcha in a comment containing `console.log(` that a grep would false-positive on; flags any stdout-writing `console.*` (log/info/debug/dir/table/…) and points to the correct `console.warn` pattern in `aemo-per-plant.json.ts`. Wired into `ci:gates` + a `ci.yml` step. (2) **Build-output gate** `scripts/ci/check-build-output-json.ts` (pure walker `scripts/lib/json-output-validate.ts`) — asserts every `dist/_file/**/*.json` parses; wired as `postbuild` so it fails the Vercel build (`vercel.json` buildCommand `npm run build`) before a broken `dist/` can deploy. Tests: `tests/loader-stdout-safety.test.ts` (12) + `tests/build-output-json.test.ts` (4). Verified: gate 1 catches the real bug on `origin/main` (`ontario-per-plant.json.ts:377`) and passes on the fix branch; typecheck + 981 vitest + `ci:gates` all green. **Branched off the #259 fix branch** (main still carries the bug), so the PR diff temporarily includes #259's one-liner until #259 merges.

**BUILT + VERIFIED 2026-06-20 (PR pending) — Peru per-plant rebuild.** Re-adds the Peru per-plant regions that #247 dropped, as **12 real COES-dispatched plants** (7 solar: sunny/san-martin/rubi/clemesi/intipampa/matarani/majes; 5 wind: punta-lomitas/wayra/san-juan/tres-hermanas/cupisnique), all **`tier: estimated` / modelled-fallback**. Investigation overturned the drop note's premises: COES is **not geo-blocked**, it **cloud-IP-blocks** (residential abed-NZ reaches it fine; the `"1"` is COES's POST-`/exportar` success flag, not a block sentinel), and **4 of the 5 originally-named plants were phantom/wrong-fuel** (never validated before the drop). No tier elevation — COES has no measured per-plant curtailment, so magnitude = generation × 2% national calibration (same as national `peru.json.ts`). Architecture mirrors Colombia: `scripts/relay/peru-coes-fetch.py` (residential relay) → committed `data/historical/peru-coes-per-plant.csv` → registry-gated `src/data/peru-per-plant.json.ts` (reads CSV at build, emits exactly the 12 declared ids — no over-emission). Wired all 5 files (regions.ts, tests, STATIC_PROFILE_KIND, tier-counts golden, 12 validation docs) + index.md. **Tally golden 461 → 473 (T3 238 → 250).** Local gates green: typecheck + 965 vitest + ci:gates + validate. Follow-up: no auto-refresh cron yet (one-shot CSV; needs an abed timer + commit path). **NB: this section's "What's shipped" tally (431) is stale — the authoritative gated count is the golden `tier-counts.json`, now 473.**

**IN FLIGHT 2026-06-17 — Honest world-expansion (branch `fix/world-expansion-honest`; supersedes #210/#215).** Rebuilds the world-expansion that #215 gutted, keeping only additions that survive the no-fabrication bar. **Shipped: estonia → `estonia-wind` + `estonia-solar`** (both live T1a, real ENTSO-E B19/B16, non-zero) and **south-korea → `south-korea-solar` + `south-korea-wind`** (T3 modelled anchor split; EPSIS returns 0% headless so the loader falls back to published IEA anchors — solar reads 0 at KST night by construction, no fabricated data). Net +2 → 440.
- **Rejected from v2 after audit (the institutional point):** `norway-no{1..4}-solar` (Norway at 58-71°N has negligible PV; `no3-solar` was all-zero, `no2/no4-solar` were phantom rows the loader never emitted); `uruguay` split into wind+solar (main's `uruguay` is an all-zero ADME placeholder already in the zero-allowlist — splitting it just makes two zeros); `bosnia-and-herzegovina-wind` (0.00012 TWh) + `montenegro-wind` (0.00064 TWh) (real ENTSO-E but 10-50× below the dataset's smallest meaningful signal, estonia-wind 0.0019 — map clutter without analytical value); `bosnia-and-herzegovina-solar` (all-zero). All four reverted to `main`; their all-zero aggregates stay as-is (already allowlisted).
- Gates: 928 vitest, `ci:gates`, `validate`, typecheck all green. Goldens (`tier-counts.json` 440, `magnitude-baseline.json` estonia split) + 4 validation docs updated; `estonia.md`/`south-korea.md` orphans deleted.
**IN FLIGHT 2026-06-17 — Japan per-fuel split (branch `feat/japan-fuel-split`).** Splits `japan-hokkaido` + `japan-tohoku` from single summed `kind:solar` regions into `-solar` + `-wind` (the loader already parses 太陽光 / 風力 separately; it was summing them). **+2 → 440.** A 30-day measurement justified only these two: Hokkaido wind 16% / 3.5 GWh, Tohoku wind 12% / 13.5 GWh — both above the 1 GWh floor. **Hokuriku deliberately NOT split** — its current live wind curtailment is 13 MWh/30d, far below the floor (and its solar dropped 21× vs baseline, anomalous — flagged for a feed check). The other 7 areas stay solar-only (<5% wind). `runJapanAreaLoader` for the 7 untouched; new `runJapanAreaLoaderSplit` builds two RegionData from the same points. All gates + 927 tests green.

**IN FLIGHT 2026-06-17 — Flare oilfield split (branch `feat/flare-oilfields`).** Splits the two aggregate flare regions into named oilfields and adds new flare coverage, from the World Bank GFMR 2025 per-flare-site VIIRS dataset (2024 rows), all at the repo's `bcm × 3.6925 TWh-e/bcm` electrical-equivalent factor. **+26 anchored/flare regions → 468** (stacked on #217 +2 and #221 Japan +2; landed in that order). T2-flare 8→34.
- **S. Iraq**: 12 named fields (Rumaila 10.6, W.Qurna-2 7.5, Zubair 6.3, Buzurgan 5.0 TWh-e…); `s-iraq` re-anchored 63→6.7 as the bbox residual bucket.
- **N. Iraq** (new coverage, Kirkuk/Kurdistan): 4 named + `n-iraq` residual bucket (+14.6).
- **E. Saudi**: 6 named (Ghawar 2.7, Jafurah 1.2…); `e-saudi` re-anchored 8.1→2.2 residual.
- **Nigeria/Algeria/Libya** national flare: +23.9 / +29.1 / +23.2.
- Net flagship flare total grows ~+80 TWh-e (all GFMR-measured). Cross-checks reconcile to the bbox anchors within <0.01%. All gates + 952 tests green. Marjan (offshore) dot placed at its Tanajib onshore base to satisfy country-containment.

**SHIPPED 2026-06-16 — Granularity & gaps survey, coverage-audit v2 (PR #163).**
- Schema v2 (`scripts/validation/coverage_audit_schema.py`): +`parent_region_id`/`granularity_available`/`expected_new_regions`, split-row scoring branch (no already-modelled penalty); v1 world CSV migrated to 20 cols, scores byte-stable (regression-tested). 35 pytest green.
- `data/coverage-audit/2026-06-10-granularity-and-gaps.csv` — 143 candidates (32 split / 111 gap) across 10 research lanes, lint-clean; top-15 cold-verified **15/15 confirmed, 0 downgraded**. Standout: the World Bank GGFR per-site flare XLSX carries a populated `Field Name` column → `s-iraq`/`e-saudi` can split into named oilfields (~15 new flare regions); 9 EIA-930 US BAs are unwired-but-open; the dominant gap mode is TLS/egress decay, not secrecy.
- Synthesis + ranked top-20 split/gap backlog: `docs/research/2026-06-10-granularity-and-gaps.md`. **Implementation PRs still to come, top-ranked first; each split PR walks the 5-file tier checklist + magnitude `--update`.**
- No TS surface touched; pure additive audit tooling + data + docs. Spec/plan: `docs/superpowers/specs|plans/2026-06-10-granularity-*`.

**Survey-backlog implementation (in progress 2026-06-17) — sequenced PRs off the #163 ranked backlog.** Decisions locked: EIA-930 BAs ship as **T2 `anchored`** (not the survey's "T1" — `live-data-paths.md` Path-B/Test-2 says Ember-anchored rates are T2); flare splits = **~13 named fields** (10 Iraq + 3 Saudi; Saudi's 8 TWh bbox is too small for 5 dots); **Japan = no-op** (the per-fuel split already flows via `fuelShare`).
- **PR #1 — EIA-930 9 second-tier US BAs (SOCO, PACW, PACE, PSCO, AZPS, SRP, IPCO, TEPC, WACM) → 18 T2 regions.** Reuses `buildEiaIsoRegionPerFuel` (canonical `regions.ts` tier overrides the helper's `live` default via `enrichWithTier`). Region count **385 → 403**, T2 6 → 24. All gates + 869 tests green.
- **PR #2 — Italy CNOR/CSUD/SUD/CALA bidding zones → 8 T1b regions.** ENTSO-E A75 live-probed 2026-06-17 (both B16/B19 non-empty); completes Italy's 7-zone partition (was 3 of 7). Region count **403 → 411**, T1b 10 → 18. Targeted `entsoe.json` merge (8 new zones cherry-picked from a full loader run) + 8 sorted magnitude baselines — zero churn to other European zones. All gates + 877 tests green.
- **PR #3 — Germany DE-LU → 4 TSO control areas → 8 T1b regions.** ENTSO-E A75 CTA-EICs live-probed 2026-06-17: all 4 return B16(solar)+B19(onshore wind); B18(offshore) only the 2 coastal CAs (50Hertz, TenneT), applied accordingly. Replaced the germany-wind/germany-solar **aggregate** (T1a) with 8 per-CTA T1b regions. **Region count 431 → 437** (T1a 149→147, T1b 18→26). EIC↔TSO mapping pinned (`10YDE-EON`=TenneT, `10YDE-VE`=50Hertz — the historical-name trap). All gates + 925 tests green.
- **Parallel author-PRs reviewed + dispositioned (2026-06-17):** #203 China/India fuel-splits (+15 T3 → 426) and #206 Chile/Colombia/DR/Ukraine (+5 T3 → 431) reviewed non-destructive/additive/accurate, rebased onto current main, goldens recomputed, snapshots+docs generated, **merged**. #204 (Mexico T3→T2 over-claim with no anchor + a synthetic placeholder CSV) and #210 (fails `tsc` + half-wired Korea split + missing docs) **bounced to authors** with specific fix lists.
- **Still to come:** GGFR flare splits — the last survey-backlog item (~13 named oilfields splitting `s-iraq`/`e-saudi` + Nigeria/Algeria/Libya T3 flare; static anchors from the GGFR `Field Name` XLSX, which needs a local download to extract per-field volumes).
- **Deferred to a single end-of-rollout sweep:** the cosmetic "385 regions" prose count in README / dataset docs / paper (ungated; updating it once at the end beats 4× churn).

**SHIPPED 2026-06-16 — Accuracy gates (PR #149).** Three CI-enforced safeguards against the worst shipped bug classes (Brazil-ONS cap-as-curtailment eabf8e5; Hokkaido 万kW 10× overcount):
- **Magnitude-drift golden gate** — `scripts/ci/check-magnitude-golden.ts` locks every live-tier region's snapshot `totalTWh` to a committed baseline (`scripts/ci/golden/magnitude-baseline.json`) within a factor-4 band; `--update` re-baselines (commit = the review trail), `--self-test` covers the drift logic. Wired into `ci:gates` + `verify`.
- **Zero-allowlist expiry** — `KNOWN_ZERO_LIVE_ALLOWLIST` extracted to `scripts/lib/zero-allowlist.ts`; every entry carries `addedDate`+`reviewBy`, and `npm run validate` fails on expired entries until a human re-confirms the zero. The all-zero check can no longer be masked forever.
- **Exact unit-conversion pins** — `tests/unit-conversions.test.ts`: exact-equality regression pins on the MW→GW, MWh→TWh, GW→EH/s constants.

**SHIPPED 2026-06-18 — flare gas purged (reverses the parked renewables-only decision).** The owner reversed the earlier "keep flare on `main`, build renewables-only as a separate product" call: flare gas is a holdover from an earlier concept that doesn't fit the narrative, so it was removed from the canonical dataset and the live site. Removed all 37 flare regions + the `T2-flare` bucket + the UI toggle/footnote/globe-filter + `fuel.ts`/`statics`/`types` wiring + 37 validation docs + the dataset schema `flare` enum; minimal prose trims to `about.md`/`methodology.md`. Total 468→431. `version-history.csv` history preserved (records what each published version shipped) + a `dataset/CHANGELOG.md` `[Unreleased]` note. **Deferred follow-ups:** the academic paper reframe (`docs/paper/`, `docs/dari/`, `src/paper.md` — still flare-dominant, knowingly inconsistent until reframed) and cutting a flare-free Zenodo v1.4.0. The old drafts #153 (`codex/remove-flare-gas`) and #237 (`remove-flare`) are superseded. Design spec: `docs/superpowers/specs/2026-06-18-flare-gas-purge-design.md`.

**Previously shipped — CI automation fix (#135, verified by bot PRs #136/#137), 2026-06-09.** The two scheduled committers — `Historical snapshot append` and `Relay pull (Colombia + India)` — had been failing on *every* run: `main`'s required `verify` status check rejects direct bot pushes (`GH006 … Required status check "verify" is expected`), and relay-pull additionally lacked `contents: write` (403). Both workflows now commit to a bot branch, open a PR, and `--auto --squash` merge once `verify` is green; `[skip ci]` dropped from the relay commit so `verify` actually runs. `ci.yml` also triggers on `push: automation/**` so the bot's branch-push deterministically produces the `verify` check — the `pull_request: opened` event can race the push-then-create and silently drop its run (observed on PR #135). **`AUTOMATION_TOKEN` secret created** — fine-grained PAT, owner honeybeesquad, scoped to this repo only, Contents + Pull requests write, **no expiration** (set 2026-06-09 per owner's call — trades the standing-credential risk for zero silent breakage; revoke/regenerate manually via token id 15584863 if it ever leaks). The default `GITHUB_TOKEN` deliberately cannot trigger `verify`, which is why a PAT is required. `allow_auto_merge` enabled repo-wide 2026-06-09. **Verified end-to-end:** both workflows dispatched → PRs auto-merged (#136 relay CSVs Colombia 1700/India 309 rows, #137 history snapshot). Unrelated failing workflows in the original report (`Deploy`, `Portal Monitor + Data Update`) belong to the separate `every-last-particle` repo.

The 2026-06-07/08 session merged **#128 #129 #130 #131 #132** (globe encoding · version-history · housekeeping · relay-resilience · Colombia data-spine). Forward work for the data-spine is captured in `docs/superpowers/plans/2026-06-08-colombia-data-spine-next-steps.md` (handoff). Earlier 2026-06-06/07 session merges/closures:
- **#119** loader-resilience (Norway NO5 B11+B12, NYISO solar-gap, sourceStatus, serbia/nmk demotion) — merged.
- **#109** paper v1.3.2 numbers refresh — merged.
- **#120** STATUS refresh — merged.
- **#108** claim-cascade Figure 1 — merged (SVG regenerated to v1.3.2 numbers / 385 regions before merge).
- **#121** v1.3.2 version bump + CHANGELOG — merged; tag `v1.3.2` + GitHub release published → Zenodo minted version DOI `10.5281/zenodo.20570864` (2026-06-07).
- **#125** fix(loaders): norway-no5 live→estimated (Statnett not reporting A75) — merged.
- **#105** Vercel-bot analytics draft — closed (redundant; analytics already on main via dynamic-inject, commits 36a602e / 7dcf2e8 / 6ce4c7e).

Also cleaned this session: 16 merged remote branches + 4 session branches deleted; 3 snapshot-only stashes dropped (6 source-bearing stashes left for review). PR #68 remains superseded (pricing layer deleted in PR #87).

## Known follow-ups

**Closed by the 2026-05-10 follow-up sprint:**
- ✅ Pillar-coord bugs (guinea, guatemala-siepac) — PR #89
- ✅ Island-polygon artifacts (japan-okinawa, jeju, vanuatu) — PR #91 (override polygons)
- ✅ Japan upstream availability — PR #90 (3 tier downgrades + investigation)
- ✅ `build_region_docs.py` manual-block markers — PR #92

**Still outstanding:**
- **Region pages — the three things deliberately left out (2026-09-06).** (a) **No link from the dashboard.** Nothing on `/` points at `/regions` or `/region/<id>`; the surfaces are reachable only by URL and via `sitemap.xml`. `src/index.md` was being rewritten in parallel when they shipped, so the hotspot rows were left alone — wiring a hotspot row to `./region/<id>` and adding a "Provenance and validation" link to `src/components/region-tooltip.js` are the two obvious follow-ups. (b) **No `<link rel="canonical">` or per-page `<meta name="description">`.** Front-matter `head` *replaces* the config `head` (Framework's `getHtml`), which would drop the no-FOUC theme boot script, so per-page meta needs `head` in `observablehq.config.ts` to become a `({path}) => string` function. Worth doing before these URLs are cited. (c) **Region ids are now citable URLs.** `/region/<id>` is public and ids do get renamed here — `india-north` and `japan` already have — so a rename now breaks an external link. That needs a redirect policy; `redirects` in `vercel.json` is the cheap version.
- **🔁 Refactor session in flight (2026-06-17) — full ledger + next steps in `docs/research/2026-06-17-refactor-session-handoff.md`.** Lighter-deps: #230 (react-dom) merged; #233 (@vercel/analytics+react) ready. Page dedup: #234 extracts `finalizeRegionData` (and makes embed's integrity check non-fatal). Globe split Step 1: #235 (`globe-geo.ts` pure helpers); plan in `docs/research/2026-06-17-globe-split-plan.md`.
- ✅ **`embed/globe.md` region drift — RESOLVED 2026-09-06** (see the 2026-09-06 entry at the top). The embed was indeed broken in production, but not for the reason this bullet predicted: the integrity check has been non-fatal since #234, and the visible break was a missing comma introduced on 2026-08-21. Drift is now closed by exact `regionData` parity with `src/index.md`, enforced by `tests/globe-drift.test.ts`. **Still outstanding from the hand-off doc:** the shared `buildRegionData()` extraction, and replacing the positional `Promise.all` destructuring in both pages with a name-keyed map — the pattern that caused the 3-month silent region-swap described in the 2026-09-06 entry.
- **⚠️ Rotate leaked/at-risk secrets (audit SEC-1/2/3, confirmed live 2026-06-16).** `EIA_API_KEY` is leaked in git history and still unrotated — but it's a free, read-only public-data key, so impact is low despite wide exposure. The genuinely sensitive creds (ERCOT username/password/key, DeepSeek key) sit unencrypted in `.env.local` but were **not** committed to history; the Vercel JWT in `.vercel/.env.production.local` is short-lived. Severity ranking + per-secret rotation steps + history-scrub procedure: `docs/ops/secret-rotation-runbook.md`; placeholder template: `.env.example`. **Rotation needs human portal access** (an agent can't complete it).
- **Colombia data-spine — next steps + open decisions:** see the handoff `docs/superpowers/plans/2026-06-08-colombia-data-spine-next-steps.md`. Tracks: (A) hardening — backfill history, weekly prev-month refresh, retire Britta, object-storage sync; (B) siting — coordinate crosswalk, pick curtailment signal, write Spec 3; (C) trivial batch — flare expansion, bad-conversions gate (needs 80%/100% decision), EIA fixture test. **Security: rotate the abed login password** (exposed in the 2026-06-07 transcript; SSH is key-based so it won't lock the agent out).
- **Recalibrate north-macedonia-solar anchor** — current 0.02 TWh/yr static (IRENA RCS 2025, 833 MW end-2024 basis) is a known underestimate; NMK hit ~1.2 GW by end-2025 with solar already moving power-exchange prices. Revisit if a machine-readable MEPSO/exchange curtailment source appears. (serbia-solar 0.007 TWh/yr is fine — curtailment genuinely negligible at 241–318 MW per USEA 2022.)
- **1 stash left for review** (`stash@{0}`, formerly `{5}`: `chore/paper-post-council-edits` — a `docs/paper/03-data-records.md` rewrite, −69/+25). The other 5 (snapshot/tally churn from already-shipped branches + a superseded `src/style.css` tweak) were dropped 2026-06-16. Review and apply-or-drop the paper stash when convenient.
- ✅ **Cited validation docs re-wrapped in manual-block markers (2026-06-16).** The 5 bad-conversion blocks lost in the `e4db3d6` doc-regen (`alberta-wind` + `india-{andhra-pradesh,gujarat,maharashtra,tamil-nadu}`) were restored from `e4db3d6^` and wrapped rajasthan-style — `<!-- BEGIN MANUAL -->` *above* the `## Bad-conversions check` heading so the block anchors to `## Published anchors` (a generated heading) and survives future regens. Root cause of the original loss: the markers had sat *below* the section heading, anchoring to a non-generated heading that the regen dropped. `data/coverage-audit/bad-conversion-citations.json` re-baselined 2→7. (`india-karnataka` already matched via inline text; `india-rajasthan` was already wrapped in PR #92.)
- **End-to-end loader-output integrity test** — deferred to Phase 5 of the audit-fix plan. Needs the wiring logic factored out of `src/index.md`'s inline Observable cell into a callable `loaders.ts` module, then a test that exercises the full loader → wiring → `regionData` chain.
- **`mountGlobe` split** — the ~390-line `render()` inside a 747-line closure. Now scoped: `docs/research/2026-06-17-globe-split-plan.md`. Step 1 shipped (#235 — pure helpers `wrapLongitude`/`easeOutCubic` → `src/lib/globe-geo.ts`). Steps 2–3 (`generateDotGrid`, `nearestPillarUnit`) are safe-next; Steps 4–6 (draw-layer + pillar split) need a headless render harness first.
- **TEPCO monthly CSV migration** (loader rewrite) — would restore TEPCO from `tier: "estimated"` to `T1a-live-tso`. The viable file `eria_jukyu_YYYYMM_03.csv` has a direct `太陽光出力制御量` (solar curtailment) column — better data quality than the abandoned 5-min path. YYYYMM URL scheme + 30-min intervals + multi-column parse needed.

**Closed by the 2026-05-12 launch-prep sprint:**
- ✅ **Issue #44** — flare regions render as solar-yellow when the flare-gas toggle is on. Verified fixed in `src/lib/fuel.ts::getRegionFuelColor` (the `region.kind === "flare"` short-circuit returns the flare token before the `dominantFuel` fall-through can paint it yellow).
- ✅ Stale "Sunfire/Vellum/Eclipse" theme references in `src/lib/fuel.ts` and `src/lib/theme-tokens.ts` corrected — shipped themes are Sunfire and Deepcurrent. (Vellum and Eclipse were never shipped.)
- ✅ Duplicate `iso-ne` / `nyiso` entries removed from `KNOWN_AGGREGATE_IDS` in `scripts/ci/check-tier-coherence.ts`.

**Pre-existing:**
- **Issue #43** — India SLDC live parsers. Needs Mullvad (or equivalent) with a genuine India PoP before the 3 geoblocked SLDCs from PR #74 can be wired live. Note: the SLDC ingestion path now exists (PR #88), it just has no data to ingest yet.
- Safari new-tab theme-persistence quirk (Bug 3 from earlier Phase 7 work — still needs Tab B/C reload reproduction).

## Plans archive

Shipped plans live in `docs/superpowers/plans/archive/`. Active plans (if any) live in `docs/superpowers/plans/`. **A plan being on disk does NOT mean it's still live work** — check this file or git log before acting on a plan you find.

## Worktree hygiene

- Rule: before creating any branch, run `git branch -a | grep <prefix>` and `gh pr list --state all --search <prefix>`. If a branch with similar name exists, ask the user before reusing it.
- Rule: stale worktrees are evidence to the next session that work is mid-flight. Tear them down when done — don't leave them around as breadcrumbs that mislead future Claudes.
- Run `git worktree list` to see current state. Most worktrees are dispatch artefacts from earlier phases; not all represent live work. If unsure whether a worktree is active, ask the user before touching its branch.
